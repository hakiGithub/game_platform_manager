# MERC-3 UI 设计说明 · 部署扩展步骤与目标版本选择

| 项 | 值 |
| --- | --- |
| Issue | MERC-3 支持部署期自动化扩展步骤，并落地 dnf-tw 指定版本部署 |
| 事实来源 | `docs/prd/MERC-3/prd.md` @ `270f9d0`（v0.5，分支 `agent/productmanager/merc-3`）—— 主要依据 §6.1 / §6.2 / §8.4.2 / §8.5 / §8.6 / §12 / §16.2 OP-05 |
| 决策来源 | ADR-0029 @ `590af8d`（分支 `agent/leader/chat-1cfa252af963`）决策 1–9，未改写 |
| 视觉基线 | `frontend/src/views/instance/deploy.vue`、`frontend/src/components/DeployProgress.vue`、`frontend/src/styles/variables.scss`、`docs/design/ui-design-spec.md` |
| 原型 | 同目录 `index.html`（浏览器直接打开，左侧切换 25 个状态） |
| 本版结论 | **v0.2**：OP-05 三项定稿不变（§2），评审第 1 轮 4 项阻断 + 7 项建议全部处置，落点见 **§13 评审条目 → 落点**。`displayName` 选项文本口径按 Leader 裁定 1 定稿（§6.1）；S1 摘要行的裁定（FR-04 加限定「目录可用时」）已并入 §5 / §10-A。 |

> 本文只定「界面上出现什么词、出现在哪、什么状态下不出现」。步骤标识与耗时的**承载字段**属 PRD §8.5 的日志呈现契约，归 @Architect；本文给的是**词面**与**渲染位置**，两者以「契约下渲染出的文本与本文 §6 一致」为符合判据。

---

## 1. 占位符约定与硬约束自检

本文与 `index.html` 内所有 **`〈…〉`** 均为**字段占位符**，不对应任何真实版号、下载来源或目标路径。

| 占位符 | 含义 | 数据出处 |
| --- | --- | --- |
| `〈versionId〉` | 版本目录条目的版号（选项副标 / 摘要 / 日志通用槽位） | PRD §8.1 `versionId`（人类 Owner 后续提供，OP-02 缺口期不存在真实值） |
| `〈versionId ①〉` `〈versionId ②〉` `〈默认条目 versionId〉` | 原型内三条目录条目各自的版号（① 声明了展示名、② 未声明、默认条目恒显「默认版本」） | 同上 |
| `〈展示名 ①〉` | 条目 ① 的 `displayName` | PRD §8.1 `displayName`（可选，缺省 = `versionId`；文案归 @Designer，见 §6.1） |
| `〈非默认版本 ①〉` `〈非默认版本 ②〉` | v0.1 的旧槽位，**本版起废弃**，一律由「`〈展示名 N〉` + `〈versionId N〉` 副标」两行式取代 | — |
| `〈步骤标签〉` | 步骤声明的 `label` | PRD §8.2 / §8.3 |
| `〈补丁来源〉` | 补丁 URL | PRD §8.2 `url`（本期不出现真实值） |
| `〈部署路径〉` | 实例安装目录 | `deploy.vue:297` 既有生成规则 |
| `〈目标路径〉` `〈备份标识〉` `〈sha256〉` `〈timeoutMs〉` `〈端口〉` `〈值〉` `〈主机名〉` `〈原因〉` `〈校验失败要点〉` | 各自字段 | 声明侧或运行时值 |

复扫口径（对 `index.html` 与本文件执行，结论见下）：

| 扫描项 | 判据 | 结果 |
| --- | --- | --- |
| 下载来源 | `http[s]*://` 字面量 | **0** |
| 版号 | 上游镜像 tag 字面量、游戏模板内的镜像名、任何 `x\.y\.z` 形态版本号 | **0** |
| 目标路径 | `/opt` `/data` `/home/<user>` `/var/lib` 及 compose 内任何容器路径 | **0** |
| 变量默认值 | `dnf_tw.yml` 的 `variables[].defaultValue` 字面量（含密钥类） | **0**（界面内一律 `〈值〉` 或 `******`） |

注：本表只列**判据**、不复制被禁字面量本身，以免文件通不过自己声明的扫描（本文件内 `x\.y\.z` 的命中均为文档节号，如 §8.4.2）。界面中出现的版本类文字只有设计定稿的通用词「默认版本」——BR-02 规定默认版本沿用游戏模板内既有 tag、无独立版号，故本就不该显示 tag 字面量（§2-①）。

---

## 2. OP-05 定稿结论（交 Leader 统一回写 §6.1 / §6.2）

### ① 版本选择项落位：**部署向导步骤 2「选择游戏」右侧详情面板，紧随「部署方式」之后**

PRD §6.1 允许落在步骤 2 或步骤 3，本文取**步骤 2**，理由按权重排：

1. **目录的作用域由 gameCode + deployType 共同决定**（PRD §8.1 声明读取期、§6.1 步骤 2「切换部署方式需重取目录（F-08）」）。而部署方式这一控件只在步骤 2 存在。若版本项落在步骤 3，用户在步骤 2 改部署方式时，会**在视线之外**改掉步骤 3 已填的值——跨步隐式失效是最难被用户发现的交互缺陷。落在步骤 2，重取与回落都发生在同一屏、同一字段上，因与果相邻可见（原型 W4 / W6）。
2. **语义归属正确**：版本回答的是「部署哪一个版本」，与「部署方式」同类；步骤 3 是元数据 `configSchema` / `variables` 驱动的**零代码生成表单**（ADR-0002 范围隔离、ui-design-spec 3.3 步骤3）。把一个人工控件塞进自动生成表单区，会让该区块的渲染规则出现例外分支。
3. **满足 PRD 对步骤 3 的唯一硬约束**「提交前必须可见并可改」：控件在步骤 2，「可改」由既有「上一步」回溯满足（该步内容全程 `v-show`、不卸载，改值不丢失）。

配套结论：

- **区块形态**：沿用步骤 2 既有 `.detail-section`（`section-title` 13px/650 + 下边框 + 16px 下边距），控件为 `el-select`，宽度沿用既有 `el-select` 的 `width: 300px` 惯例（`deploy.vue:1425`）。不新增卡片、不新增步骤、不改 `steps` 数组。
- **默认项文案恒为「默认版本」**四字，**不显示镜像 tag 字面量**——BR-02 定义默认版本 = 沿用游戏模板内既有 tag，本就没有独立版号可展示；显示 tag 反而会把 F-09 的内部事实暴露成用户可选信息。该条目即使声明了 `displayName` 也**有意不采用**（§6.1 已明写）。
- **选项文本 = `displayName`（缺省回退 `versionId`），且 `versionId` 恒以 mono 副标同时呈现**（Leader 裁定 1，口径与 AC-01 的机械核对对象见 §6.1）。
- **单值目录不隐藏控件**（PRD §12「是否隐藏单值控件归 @Designer」）：控件是否出现只由**一个**谓词「目录是否可用」决定，条目数只决定选项多寡。若改成按条目数决定，插件维护者增删条目会让控件反复出现/消失，用户也失去「版本在这里改」的稳定落点。单值态等价 S2（不写键、不进扩展阶段），与 §12 系统表现列一致。原型 W5 现以下拉**展开态**直接呈现「只有一个可选值」。

### ② 扩展阶段用户可见名称：**「部署扩展」**

| 候选 | 判定 | 理由 |
| --- | --- | --- |
| **部署扩展** | **采纳** | 与 `glossary.md` 的领域术语「部署扩展阶段（Deploy Extension Stage）」「部署扩展步骤（Deploy Extension Step）」逐字一致；对用户而言「部署」是已知语境词，「扩展」表达"在部署之内的额外处理"而不承诺具体手段。文档、日志、界面同一词，运维讨论时可直接引用界面文字（该词是核对 AC-16 / KPI-02 的对象，词面分裂会直接产生歧义）。 |
| 版本定制 / 版本处理 | 否 | 只对 dnf-tw 的首个用例成立。框架必须能承载与版本无关的扩展步骤（决策 1、G-01），用「版本」命名会对那些步骤说谎。 |
| 补丁与脚本 | 否 | 暴露实现种类；将来新增步骤类型即名不副实；且种类词已在**步骤行**呈现（§6.2），阶段名再重复一层是噪声。 |

配套：两类步骤的用户可见名称定为 **「补丁替换」（PATCH）** / **「脚本执行」（SCRIPT）**；步骤副属性用平实措辞 **「失败即终止」/「失败可继续」** 表达 `fatal`，界面不出现 "fatal" 字样（§8 的字段名是声明侧语言，不是用户语言）。

### ③ 失败提示文案：见 §6 文案矩阵

三项要求全部落定：(a) BR-12 两类可辨识原因的词面已分别定稿并保证「只靠两处长短不同的短语即可区分」；(b) 三类目录异常态（未填充 / 不合法 / 不可用且键既存）的措辞互斥，不可互相冒充；(c) 全部措辞不暗示界面上不存在的操作（对齐 PRD v0.4 对 §12 的收口）。

---

## 3. 页面与模块结构

本期只动两处界面，第三处为新增呈现。`index.html` 左侧导航按此分组。

### 3.0 DESIGN-ID 清单与需求映射

| DESIGN-ID | 设计物 | 所在屏（原型场景号） | 需求 / 规则 | 验收 |
| --- | --- | --- | --- | --- |
| **D-01** | 步骤 2「目标版本」区块（控件 + 选项文本 displayName/versionId + 默认项文案 + 徽标） | W2 / W3 / W4 / W5 | FR-01、BR-02、§8.1 | AC-01（核对口径见 §6.1） |
| **D-01a** | 该区块的**不渲染**形态（空目录 / 无插件 / 不合法且无键） | W1 | FR-02、FR-23、§8.6、RISK-13 | AC-02、AC-24 ①③ |
| **D-01b** | 目录读取加载态 | W2 | FR-01（可用性前提：读取中不得出现空选项） | — |
| **D-01c** | 单值目录不隐藏控件（下拉展开、看得见「只有一个可选值」） | W5 | §12「目录仅默认版本」行（OP-05 交拍项） | — |
| **D-01d** | 部署方式切换 → 重取目录与回落（含 `ElMessage.info` 实画帧） | W6 | §6.1 步骤 2、F-08、X-02 | AC-01 |
| **D-01e** | 部署扩展步骤预览（含「失败即终止 / 失败可继续」词面与 label 缺省回退） | W4 | FR-05、FR-06、BR-04；N-09 边界 | AC-16 |
| **D-02** | 步骤 5 摘要「目标版本」行（条件渲染 + 取值规则 + `versionId` 副标） | **W7 / W8b / W8** | FR-04、§8.4.2 三态 | AC-01、AC-22（S1 取值已由 Leader 裁定，见 §10-A） |
| **D-04** | 既存键 + 目录不可用时摘要行的诚实呈现 | W9 | BR-12、FR-13、§12「向导不展示但键已存在」行 | AC-20 (b) |
| **D-03** | 部署日志扩展阶段呈现（阶段带 + 「扩展」步骤点 + `statusText`） | LA / **LJ** / LB | FR-10、FR-20、BR-10 | AC-03、AC-04、AC-16、KPI-02 |
| **D-03a** | 步骤三行词面（开始 / 成功 / 失败 + 耗时格式） | LA / LJ / LC / LD | FR-12、FR-15、FR-20 | AC-03、AC-06、AC-12 |
| **D-05** | 失败与拦截文案矩阵（含 BR-12 两分支） | LE / LF / LG / LH / LI | FR-17、FR-18、FR-19、BR-12、BR-14、§8.1 | AC-08、AC-09、AC-10、AC-11、AC-20、AC-21 |
| **D-06** | 步骤 4 环境校验的**不变**约束（负向设计） | WA | N-09、§6.1 步骤 4 | AC-15 |
| **D-07** | 视觉可辨识性前提（level → 颜色/图标；阶段带不靠单一颜色）与空日志态 | **E3（空态）/ E4（着色）** | ui-design-spec 2.2.3；FR-17/FR-18 可见性；§4.2 T | AC-08、AC-10、AC-16（见 §10-B1）、AC-15 |
| **D-08** | 不改动清单（实例列表/详情、成功弹窗、插件页） | §3.3 | N-03、N-07、N-10、BR-10 | AC-15、AC-24 |

### 3.1 部署向导 `frontend/src/views/instance/deploy.vue`（真实为 5 步，非 ui-design-spec 3.3 所述 4 步）

| 步骤 | 名称（代码 `steps[]`） | 本期 | 设计 |
| --- | --- | --- | --- |
| 1 | 选择主机 | 不变 | — |
| 2 | 选择游戏 | **新增「目标版本」区块** | D-01 |
| 3 | 配置参数 | 不变（版本项不落在此，见 §2-①） | — |
| 4 | 环境校验 | **不变，不新增任何检查项** | D-06（负向约束） |
| 5 | 确认部署 | **新增「目标版本」摘要行（条件渲染）** | D-02 |

既有左侧 rail 的 `el-steps`、`stage-header`、页头 `header-context` 均不改动；不新增步骤、不改 `steps` 数组、不改 `stepDescriptions`。

> ui-design-spec.md §3.3 写的是「4 步向导」、且把「预校验与确认部署」合为一步；代码实为 5 步（`deploy.vue:28-34`）。PRD §6.1 的 5 行表与代码一致。**本文按代码 + PRD 执行**，并把该规范文档的偏差登记为待订正项（§10-C）。

### 3.2 部署日志 `frontend/src/components/DeployProgress.vue`

D-03 扩展阶段呈现 = 三处同时可辨识（缺一不可，AC-16 / KPI-02 的界面落点）：

1. **进度区顶部 `statusText`**：既有实现优先取服务端 `statusText`（`DeployProgress.vue:177`），故该值即为「部署扩展」。
2. **进度步骤点 `progress-steps`**：既有 5 项 `['准备','下载','安装','配置','启动']`（`DeployProgress.vue:384`）之上，**在进入扩展阶段时插入第 6 项「扩展」**，位置固定在「配置」与「启动」之间。
3. **日志流阶段带**：扩展阶段首行之前插入一条 `部署扩展` 分组带（新增视觉元素，`log-stage-band`）。**带恒在该阶段第一行（§6.2「进入部署扩展阶段」）之前**，25 屏中每一屏都成立、无例外——见 §7 X-07。

对话框宽度 `700px`、`logs-container` `max-height 340px` + mono 13px、底部按钮 `重试 / 完成 / 关闭`、进行中无控制按钮——全部保持现状。

### 3.3 明确不改动（负向清单，防被读成隐含需求）

| 界面 | 不改的理由 |
| --- | --- |
| 实例列表 / 实例详情 | PRD N-10（OP-03 裁决本期不做）：**不加版本列、不加版本徽标、不加任何读模型** |
| 向导步骤 4 环境校验 | PRD N-09 / §6.1 步骤 4：不探测目录可达性、不预探测补丁来源 |
| 步骤 3 配置参数表单 | §2-① 落位在步骤 2 |
| 部署完成弹窗 / 成功态 | 本期不改成功语义（BR-10） |
| 插件管理页 | 无 dnf-tw 前端（FR-22 / §5.1.1），不得新增 standalone 模式（ADR-0003） |

---

## 4. 状态清单（全状态覆盖）

### 4.1 目标版本控件（步骤 2）

| # | 状态 | 触发 | 界面 | 对应 |
| --- | --- | --- | --- | --- |
| A | **未填充 / 空目录**（dnf-tw 本期态） | 目录读取成功、条目 0 | **整块不渲染**（含标题与占位空间） | S1 / FR-02 / AC-24 ①③⑥ / W1 |
| B | 无插件 / 插件未加载 | 读不到目录 | 同 A | S1 / AC-02 / AC-15 |
| C | 声明不合法（未过 §8.1 校验）且无键 | 校验失败 | 同 A；解释行**只在部署日志**（D-05），向导内不加任何提示 | §8.1 ① / LG |
| D | 加载中 | 选定游戏或部署方式后读取未返回 | 占位渲染：`el-select` 禁用 + loading + 占位文案「版本目录读取中…」 | W2 |
| E | 目录可用·单值 | 仅 `default=true` | 渲染，一个可选值，不禁用不隐藏；原型 W5 以**下拉展开态**呈现，「只有一个可选值」看得见 | §2-① / W5 |
| F | 目录可用·停在默认 | 用户未选 | 渲染，选中「默认版本」 | S2 / AC-01 / AC-02 / W3 |
| G | 目录可用·选非默认 | 用户选择 | 渲染，选中该版号 + 步骤预览展开 | S3 / AC-01 / W4 |
| H | 目录不可用 + 键既存 | 插件卸载后重部署 | 控件**不**渲染（§8.4.2 S3「不改选也无法展示」），改由步骤 5 摘要承载可见性（D-02 / D-04） | §12 / AC-20 (b) / W9 |
| I | 禁用 | 未选游戏 | 区块本身不渲染（详情面板既有 `el-empty「请选择游戏查看详情」`） | 现状 |
| J | 权限不足 | 401 / 403 | 沿用平台既有登录跳转，无新增文案 | §4.2 / §12 末行 |

> **I 的第二支不可达（裁定 2）**：「已选游戏但未选部署方式」这一支**不存在**——部署方式在既有实现里恒有值：`deploy.vue:65` `selectedDeployMethod = ref("docker")`，选定游戏后 `deploy.vue:273` 回落 `supportMethods[0] || "docker"`。因此本期不为该支加任何文案或控件。

### 4.2 部署日志扩展阶段

| # | 状态 | 阶段带 | 步骤点「扩展」 | level | 对应 |
| --- | --- | --- | --- | --- | --- |
| K | 未进入（S1/S2 或未声明扩展） | 不渲染 | 不插入 | — | **AC-15 / AC-24 ③ 回归基准帧**（**LB**） |
| L | 执行中 | 渲染 | 插入并激活 | INFO | AC-16 / KPI-02（LA） |
| M | 全部成功并完成 | 渲染 + 完成行 | 完成 | SUCCESS | AC-03 / AC-04 / AC-16（**LJ**） |
| N | 致命步骤失败 | 渲染 | 终止态 | ERROR | AC-08 / AC-09（LC） |
| O | 非致命步骤失败 | 渲染 | 继续 → **照常完成**（完成终态帧） | WARN | AC-10（LD） |
| P | BR-12 拦截·本次所选 | 渲染 | 终止态 | ERROR | AC-20 (a)（LE） |
| Q | BR-12 拦截·配置既存键 | 渲染 | 终止态 | ERROR | AC-20 (b)（LF） |
| R | 回滚失败 | 渲染 | 终止态 | ERROR + 保留说明 WARN | AC-09 / BR-14（LH） |
| S | 停实例失败 | 渲染 | 终止态 | ERROR | **预留文案**，生效前提是 @Architect 对停失败定调（§14.2 行 5 / RISK-05）（E1） |
| T | 空日志（首条日志未到的轮询窗口） | 不渲染 | 不插入 | — | 既有 `el-empty「暂无日志」` 不变（**E3**，独立成屏） |
| U | 进度获取失败 | 不渲染 | 不插入 | ERROR | 既有「获取进度失败: …」不变（**E2**） |

> **LH / LI 是「词面速查帧」**：它们只为并排呈现若干失败原因词面，行集不等于一次真实部署的时序（LI 的三条 ERROR 来自三次部署）。但**凡渲染阶段带的屏，一律从「带 → 进入部署扩展阶段 → 停实例两行」起画**，速查帧也不例外——这条顺序是 AC-03 / AC-16 的机械锚点，见 §7 X-07。

**A / B / C / K 四条是本期最关键的"不出现"约束**：dnf-tw 缺口期恒处于 A+K，界面必须与改造前逐项一致。

---

## 5. 可见性判定规则（设计不变式）

把 §4 的十条状态收成一个可实现、可核对的三值规则，避免实现侧各自解释：

```
P1  控件渲染  ⇔  目录可用（读取成功 且 条目数 ≥ 1）
P2  摘要行渲染 ⇔  P1  ∨  提交载荷含 deployVersion
P3  扩展阶段呈现（阶段带 / 步骤点 / statusText）
       ⇔  本次部署真的进入了扩展阶段（未进入则一切如旧）
```

摘要行取值：

| 载荷 | 摘要显示 | 原型屏 | 依据 |
| --- | --- | --- | --- |
| 无 `deployVersion`，P1 真 | 「默认版本」+ 该条目的 `versionId` mono 副标 | **W8b** | FR-04 |
| 有 `deployVersion`，目录可用 | 主文本 `displayName`（缺省回退 `versionId`）+ `versionId` 副标 + 「本次选择」步骤数徽标 | **W8** | FR-13 / §8.4.2 S3 |
| 有 `deployVersion`，目录不可用 | `versionId` + 「沿用实例配置」标识（**目录不可用时 displayName 无从解析，只能显 `versionId`**，与日志 BR-12 行同值） | **W9** | FR-13 / §8.4.2 S3 / AC-20 |
| 无 `deployVersion`，P1 假 | **不渲染该行** | W7 | 见下（§10-A 已裁定） |

**三行取值都在原型里有对应屏**（v0.1 缺 S2 那一屏，已由 W8b 补上）。

**取值规则与选项文本同源**：摘要与下拉用同一主文本规则（`displayName ?? versionId`）+ 同一 `versionId` 副标，因此 AC-01 的「选项与目录 `versionId` 一致」在步骤 2 与步骤 5 用的是同一个机械核对对象。

**P2 的第三条（S1 不显示「默认版本」）已由 Leader 裁定采纳**：口径为给 FR-04 加限定「（目录可用时）」、§6.1 步骤 5 行改为「摘要须显示所选版本（目录可用时；无可用目录时不渲染该行）」，随 PRD v0.6 一次回写。理由：FR-04「未选择时显示默认版本」以「用户本可以选择」为前提；S1 下没有任何可选项，多出一行会让 AC-24 ④「与改造前逐项一致」不再成立。

**P2 取值的健壮性要求**：摘要行的有无与内容一律**由提交载荷推导，不由目录状态推导**。这样在「目录读取失败但键既存」的 H 态下，界面仍然诚实显示版本要求，而不是伪装成一次默认版本部署——那正是 BR-12 要杜绝的结果在界面上的翻版。

---

## 6. 文案矩阵（词面定稿）

### 6.1 名词

| 对象 | 词面 | 禁用的替代词 |
| --- | --- | --- |
| 阶段 | 部署扩展 | 版本定制、补丁与脚本、扩展中 |
| 控件标签 | 目标版本 | 游戏版本（与坏键 `gameVersion` 撞名，RISK-10）、版本目录（那是声明侧概念，不是用户选项） |
| **选项主文本** | **`displayName`；缺省时回退 `versionId`**（PRD §8.1：`displayName` 可选、默认值即 `versionId`、文案归 @Designer） | 版号字面量之外再编一套「版本别名」（如「最新版」「推荐版」）——目录里没有这个词，界面凭空造词就是假资料 |
| **选项副标（恒渲染）** | **`versionId`，mono 10px**，与主文本同行列内、主文本之下 | 省略副标；把 `versionId` 只放进 `title` / `aria-label` 等「读得到但看不见」的位置——那会让 AC-01 无法用界面核对 |
| **AC-01 的机械核对口径** | **核对对象 = 每个选项内可见的 `versionId` 副标；其集合与目录 `versionId` 集合逐项相等即通过**（`displayName` 缺省时主文本与副标同值，双行照常渲染、不合并、不省略）。摘要行（W8 / W8b）用同一口径 | 「主文本与 versionId 一致」——`displayName` 生效时该式必不等，按此核对会把正确实现判成 FAIL |
| 默认项（`default = true`） | 「默认版本」四字 + 其 `versionId` 副标。**即使该条目声明了 `displayName`，也有意吞掉不用** | `default`、镜像 tag 字面量、`（未选择）` |
| ↳ 为什么是有意为之 | BR-02 下默认版本没有独立版号可展示（它 = 沿用游戏模板内既有 tag），把它做成可被 `displayName` 改写的项，等于在界面上给「不存在的版本身份」一个名字；而显示 tag 又踩 N-12 / BR-15 ① 的不外泄口径。**这一格吞 `displayName` 是设计决定，不是遗漏，实现侧不要"顺手补上"。** | — |
| 步骤种类 PATCH | 补丁替换 | 打补丁、patch |
| 步骤种类 SCRIPT | 脚本执行 | 跑脚本、shell |
| `fatal = true` | 失败即终止 | 致命 |
| `fatal = false` | 失败可继续 | 非致命（该词保留在日志括注内，见 6.3） |
| 既存键来源 | 由既往部署写入实例配置 | 「你之前选的」「上次选择的」（用户可能不记得，且不指责任何人） |

### 6.2 阶段与步骤行

`〈…〉` 为字段槽位；`·` 前为固定词面。**行内步骤标识与耗时最终落在哪个字段由 §8.5 契约定，本节定的是渲染出来的文本。**

| 场景 | 词面 | level |
| --- | --- | --- |
| 进入阶段 | `进入部署扩展阶段` | INFO |
| 停实例前置 | `正在停止实例，确保扩展步骤在实例未运行时执行` | INFO |
| 停实例完成 | `实例已停止` | SUCCESS |
| 停实例失败 | `实例停止失败：〈原因〉 —— 扩展步骤不得在实例仍运行时执行，部署终止` | ERROR |
| 步骤开始 | `步骤 〈序号〉/〈总数〉 〈步骤标签〉 · 〈种类〉 开始` | INFO |
| 步骤成功 | `步骤 〈序号〉/〈总数〉 〈步骤标签〉 · 〈种类〉 · 成功 · 耗时 〈时长〉` | SUCCESS |
| 步骤失败·致命 | `步骤 〈序号〉/〈总数〉 〈步骤标签〉 · 〈种类〉 · 失败 · 耗时 〈时长〉 · 原因：〈原因〉` | ERROR |
| 步骤失败·非致命 | 同上加「失败（非致命）」 | WARN |
| 致命终止 | `致命步骤失败，部署终止：不再执行后续步骤，实例不启动、不交付，状态置为异常` | ERROR |
| 非致命继续 | `该步骤声明为非致命，继续执行后续步骤` | INFO |
| 阶段完成 | `部署扩展阶段完成 · 共 〈总数〉 步 · 总耗时 〈时长〉` | SUCCESS |
| 阶段交棒 | `部署扩展阶段结束，进入健康检查与启动` | INFO |

规则：

- **每条扩展阶段日志的第一行恒为「进入部署扩展阶段」，且阶段带恒在它之前**（`带 → 进入行 → 停实例行 → 步骤行`）。这条顺序就是 AC-03 / AC-16 的机械锚点，25 屏无一例外，速查帧（LH / LI）也照此画（§7 X-07）。
- **`label` 缺省时的渲染回退**：PRD §8.2 / §8.3 给声明侧的缺省值是「序号 + `PATCH` / `SCRIPT`」，那是声明侧语言，界面**不照抄**（`patch` 在 §6.1 是禁用替代词，且会与同行的种类徽标重复）。渲染侧一律回退为 **「补丁替换 〈序号〉」/「脚本执行 〈序号〉」**——即缺 `label` 时用「种类 + 序号」，绝不让 `PATCH` / `SCRIPT` 字面量出现在日志行或步骤预览里。
- **耗时格式沿用既有实现** `formattedElapsedTime`（`DeployProgress.vue:72-85`）：`N秒` / `N分N秒` / `N小时N分`。**不引入裸 ms**。这与该对话框顶部「已用时」同源，用户不需要学第二套读法。
- **`〈总数〉` 若执行期不可得**，退化为 `步骤 〈序号〉 〈步骤标签〉 · …`。总数只在阶段入口计算步骤集时可得（FR-05 / FR-12），不得为此新增接口。
- **原因段一律以「原因：」引导**并置于行尾，便于 KPI-03 归因时机械截取。
- 三行同属一步的**归组判读**以 §8.5 契约为准；本设计的阶段带提供阶段级归组，步骤级归组由契约的 `〈序号〉/〈总数〉` + 同一 `〈步骤标签〉` 词面支撑。

### 6.3 失败与拦截（含 BR-12 两类可辨识原因）

**AC-20 要求「区分是本次所选还是配置既存键」。两类文案只在两处长得不同，且这两处各自唯一：**

| 分支 | 首行（指名原因） | 追加行 |
| --- | --- | --- |
| ① 本次显式选择 | `部署终止：本次所选版本 〈versionId〉 不可用 —— 该游戏的版本目录当前不可读取或未通过声明校验` | `实例未启动、未交付，状态置为异常；该版本要求不会被静默改为默认版本` |
| ② 配置既存键 | `部署终止：实例配置要求的版本 〈versionId〉 不可用 —— 该版本要求由既往部署写入实例配置，本次未改选；而该游戏的版本目录当前不可读取或未通过声明校验` | 上一行 + `恢复路径：先恢复该游戏的版本目录（重新启用插件，或由插件维护者修复声明）后重新部署；目录恢复后如不再需要该版本，请在「选择游戏」步骤改选「默认版本」再部署（目录未恢复时该选项不存在）` |

区分锚点：**「本次所选」vs「实例配置要求的…由既往部署写入」**，以及**恢复路径行只在 ② 出现**。核对脚本按这两个锚点即可机械判定，不需读界面状态。

**这两行为什么指名 `versionId` 而不是 `displayName`**：BR-12 触发的前提正是「目录不可读取 / 未过校验」，此时 `displayName` 无从解析——日志只能报出**载荷里真实存在的那个值**（`deployVersion` = 某条目的 `versionId`）。这与 W9 摘要同口径：界面在目录可用时用展示名，在目录不可用时一律回到 `versionId`，两处报的是同一个字符串，用户才可能对得上。

**② 的措辞红线**（PRD v0.4 对 §12 的收口）：

- 禁止「在向导改选默认版本即可解锁」这类把解锁路径写成界面上不存在的操作的表述——恢复顺序必须先是「恢复版本目录」，改选默认版本是其**之后**的分支，并用括号点明「目录未恢复时该选项不存在」。
- 禁止任何「一键清除版本要求 / 回退默认版本」类控件与措辞（N-08、§12 明写本期不提供）。

**其他异常态文案：**

| 场景 | 词面 | level |
| --- | --- | --- |
| 目录不合法 + 无键（S1，不拦截） | `提示：该游戏版本目录的声明不合法（〈校验失败要点〉），已按默认版本部署，未执行部署扩展步骤` | WARN |
| 补丁来源不可达 | `原因：补丁包来源不可达〈补丁来源〉` | 按致命性 |
| 摘要不符 | `原因：补丁包摘要与声明不符（期望 〈sha256〉，实际 〈sha256〉）` | 按致命性 |
| 脚本非零退出 | `原因：脚本退出码 〈exitCode〉（非 0 即判失败）` | 按致命性 |
| 脚本超时 | `原因：脚本执行超过 〈timeoutMs〉 未返回，判失败` | 按致命性 |
| SSH 不可达 | `原因：宿主机 〈主机名〉 SSH 通道不可用 —— 〈原因〉` | 按致命性 |
| 回滚成功 | `目标路径已回滚到本次改动前状态（备份：〈备份标识〉）` | SUCCESS |
| 回滚失败 | `回滚失败：〈原因〉（备份 〈备份标识〉）。目标路径未恢复到本次改动前状态` + `平台侧不提供恢复入口，需在宿主机上手工处理后重新部署` | ERROR |
| 保留边界 | `此前已成功步骤的改动与脚本副作用一律保留，本期不提供撤销` | WARN |
| 对话框错误条（`data.error`） | `部署扩展阶段的致命步骤失败：〈步骤标签〉 · 〈种类〉。实例状态已置为异常，未启动。` | — |

**归因一致性**：KPI-03 要求每次失败归入「实现缺陷 / 排除项①声明不可用 / 排除项②资料缺失 / 排除项③外部资源」四类之一。上表中「BR-12 拦截」两行、「目录不合法」一行、「来源不可达 / 摘要不符」两行分别对应 ①、①、③——词面已确保读者能从文案直接归类，不需回查文档。

### 6.4 不渲染清单（负向文案约束）

| 条件 | 必须不出现 |
| --- | --- |
| 目录读取成功且条目 = 0（含 dnf-tw 未填充） | 版本控件、版本摘要行、「不合法」提示行、阶段带、「扩展」步骤点 |
| 停在默认版本 / 未声明扩展步骤 | 同上（其中控件仍需渲染，只是不进入扩展阶段） |
| 未登录 / 403 | 任何新增文案（沿用既有） |
| 任何界面 | 空/复数「版本列表」计数、镜像 tag、示例版号、`fatal` 字样、`PATCH` / `SCRIPT` 字面量（种类一律走中文词，含 `label` 缺省回退，见 §6.2）、步骤总数为 0 的「0 个步骤」提示、**任何「NEW / 新增 / 评审标记」类徽标（§8.4）** |

> **空目录 ≠ 声明不合法**（RISK-13）：实现不得把「读到 0 条」当「校验不通过」从而输出 §6.3 的提示行。AC-24 ③ 直接以此判定。

---

## 7. 交互规则

| # | 交互 | 规则 |
| --- | --- | --- |
| X-01 | 进入步骤 2 | 选定游戏后按 `gameCode + deployType` 读取目录；未返回前不渲染选项列表（状态 D） |
| X-02 | **切换部署方式** | 重取目录 → 若新版号仍存在则保留选择；不存在则回落「默认版本」并提示 `ElMessage.info("部署方式已切换，目标版本已重置为默认版本")`。**不得静默改选** |
| X-03 | 选择版本 | 即时刷新徽标与「步骤预览」；不触发任何校验请求（N-09） |
| X-04 | 步骤预览展开/收起 | 纯前端折叠；只读声明文本 |
| X-05 | 进入步骤 5 | 摘要行按 §5 的 P2 规则出现；摘要内**不提供**改选控件，改值走「上一步」 |
| X-06 | 开始部署 | 不因版本项而新增拦截：`canDeploy` 仍只看 `envCheckPassed`（`deploy.vue:665`） |
| X-07 | 日志阶段带插入 | 只在扩展阶段首行前插入一次；插入前已渲染行的内容与顺序不得回退。**「带 → 进入部署扩展阶段」的相对顺序本身就是核对对象：凡渲染阶段带的屏，带必须紧邻在「进入部署扩展阶段」之前，任何屏不得例外**（含 §4.2 注明的速查帧 LH / LI）。这条一旦破例，AC-03 / AC-16 的核对脚本就会在同一份设计里读到两条互斥规则 |
| X-08 | 「扩展」步骤点插入 | 在「配置」与「启动」之间插入；**五个既有点的 completed / active 判定结果在插入瞬间不得变化**（否则用户会看到进度倒退） |
| X-09 | 点「重试」 | 沿用既有行为（清空日志、重新轮询）。retry-deploy 先 `uninstall` 的后果归 §14.2 / RISK-09，非 UI 层可判定，本文不预设 |

反馈形态沿用 ui-design-spec 2.3.1：同步操作 3 秒 Message、异步操作用本对话框的实时阶段反馈，高危操作二次确认（本期**无**新增高危操作——部署扩展不属于需二次确认的高危动作，因此不引入确认弹窗，避免削弱真实高危操作的信号）。

---

## 8. 设计标注

### 8.1 Token（全部取仓内既有变量，本期不新增任何色值）

| 用途 | Token | 值 |
| --- | --- | --- |
| 页面/卡片底 | `--platform-surface-0/-1/-2/-3` | `#0b1722` `#10202d` `#142735` `#1a3040` |
| 分隔线 | `--platform-line` | `#263847` |
| 主强调（选中态、阶段状态） | `--platform-cyan` | `#27b5f3` |
| 成功 | `--platform-green` | `#52cf82` |
| 警告（扩展阶段带、既存键提示、WARN、**致命性徽标「失败即终止 / 失败可继续」**） | `--platform-amber` | `#f2b84b`（`variables.scss:221` 既有；**本期新增使用，不是本期新造**） |
| 危险（ERROR、失败态） | `--platform-red` | `#f0646a` |
| 文字 | `--platform-text-primary/-regular/-secondary/-muted` | `#e7eef5` `#c1ceda` `#8d9cac` `#68788a` |

> **本期新增的视觉元素只有两个**：`log-stage-band`（阶段带）与「扩展」步骤点的 `ext` 描边；其余全部落在既有 token 与既有组件形制上。`--platform-amber` 是**新使用位置**，token 本身早已存在——原型标注栏与本文一律按「本期新增使用」表述，不写「新增色值」。
>
> **关于 `--platform-accent` / `--platform-accent-soft`**：`deploy.vue` 有 **12 行**使用**仓内从未定义**的这两个 token（2310、2311、2414、2415、2438、2474、2511、2512、2525、2588、2603、2604），浏览器按继承/透明兜底——即当前选中态、`stage-index`、host 卡片选中标记的强调色实际未生效。`index.html` 的 `:root` **自补这两个 token，仅为还原设计意图色（取 `--platform-cyan` 同值）**：**实现侧不要照抄原型去补一个平台级 token**——那是 §10-D 登记的既有缺陷、本期不修，处理以 §14.2 的结论为准（本期新增元素直接用 `--platform-cyan`）。

### 8.2 尺寸与间距（8px 网格，对齐 ui-design-spec 2.2.4）

| 元素 | 规格 |
| --- | --- |
| 目标版本区块 | 沿用 `.detail-section{margin-bottom:20px}`；`section-title` 13px/650 + `padding-bottom:8px` + `border-bottom 1px --platform-line` |
| `el-select` | `width:300px`、`height:32px`（`deploy.vue:1416` 既有惯例）、内边距 `0 11px`、圆角 `--border-radius-base 5px`；收起态值区为「主文本 + `versionId` mono 11px 副标」两段，`gap:8px` |
| 下拉选项行 | 两行式：主文本 13px（`--platform-text-regular`，选中态 `--platform-cyan`/650）+ 下一行 `versionId` mono 10px `--platform-text-secondary`（surface-2 上 **5.46:1**）；行内右侧「含 N 个部署扩展步骤」mono 10px 同 `secondary`；行高随两行内容自然撑开，`padding:7px 12px` |
| 徽标 `el-tag` | `size small`、`effect plain`、行高 22px、与控件 `gap:12px` |
| 回落提示 `ElMessage` | 顶部居中、`info` 型（`--platform-cyan` 图标 + `--platform-text-regular` 文字）、3 秒自动消失（ui-design-spec 2.3.1）；W6 为其实画帧 |
| 步骤预览容器 | `padding:10px 12px`、`border:1px dashed --platform-line`、圆角 4px、底 `--platform-surface-2`、`margin-top:10px` |
| 步骤预览行 | mono 11px / 行高 1.5、行间距 5px、种类标签 10px + `padding:0 6px`；**序号 `.no` 与执行位置用 `--platform-text-secondary`**（surface-2 上 5.46:1）、**致命性词面 `.pos .fm`（「失败即终止」/「失败可继续」）用 `--platform-amber`**（surface-2 上 8.56:1）；「宿主机 / 容器」属执行位置、不随致命性着色 |
| 辅助说明 | `field-description` 11px、`margin-top:4px` |
| 日志阶段带 | `margin:10px 0 6px`、mono 10px、`letter-spacing:.09em`、`--platform-amber`、右侧 1px 细线 `rgba(242,184,75,.35)`；与 INFO 行的 `--platform-cyan` **色相差约 159°**（amber hue 39° / cyan hue 198°），且带内同时有「部署扩展」四字，阶段判读不依赖色觉 |
| 日志行 | 沿用 `logs-container` mono 13px / 行高 1.6 / `padding:12px 16px`；`log-time` min-width 64px、`log-level` min-width 50px、`gap:8px` |
| 摘要行 | 沿用 `el-descriptions :column="2" border`；标签格底 `--platform-surface-3` |

新增行底色（ERROR 整行浅红 `rgba(240,100,106,.07)`）沿用 `env-check-item.success/.error` 已在用的同款处理方式，属 ui-design-spec 2.2.3「整行浅红色背景高亮」的既有条款，不是新造视觉语言。

### 8.3 组件边界（前端实现契约）

| 界面元素 | 组件 | 挂载点（代码事实） |
| --- | --- | --- |
| 目标版本区块 | `.detail-section` 内 `el-select` + `el-tag` + 折叠区 | 插在「默认端口」`detail-section` 起始行之前（`deploy.vue:1063`），即紧随「部署方式」区块 |
| 摘要行 | `el-descriptions-item`（`v-if`） | 插入 `deploy.vue:1573`（「部署方式」项之后）；用 `v-if` 而非 `v-show`——S1 下不产生任何节点 |
| 阶段带 | `div.log-stage-band`（普通元素，非 el 组件） | `DeployProgress.vue:441` 的日志 `v-for` 内按条目类型分支 |
| 「扩展」步骤点 | 既有 `progress-steps` 的 `v-for` 源改为计算属性 | `DeployProgress.vue:384`（现为硬编码数组） |
| 状态文字 | 服务端 `statusText` 直显 | `DeployProgress.vue:177` 既有优先级，无需改 |
| 步骤预览 | `el-collapse` 或原生折叠 | 新区块，数据来自本次目录读取结果，不新增接口 |

不引入任何第三方 UI 库、不改全局样式（ui-design-spec 3.5.2）。

### 8.4 原型脚手架清单（**不属产品 UI，实现侧一律不输出**）

`index.html` 里以下元素只为让评审读得懂，**不是需求**。实现时照抄即属缺陷：

| 原型元素 | 是什么 | 实现侧 |
| --- | --- | --- |
| `.review-flag`（虚线 amber 标签，文字「评审标记·非产品元素」） | 标出本期新增的控件/摘要行位置 | **不输出**。「目标版本」区块标题、「目标版本」摘要行都不带任何徽标（v0.1 里这个元素叫 `.new-flag`「NEW」，未在此声明，属定稿缺项——本版改名为 `review-flag` 并写明其性质，同时 §6.4 的不渲染清单已含「界面不出现 NEW 类徽标」） |
| W1 的虚线占位框 + 「目标版本 · 本状态不渲染」标题 | 演示 S1 下**应该空着**的位置 | **不输出**任何节点（`v-if`，连标题与外边距都不产生） |
| 左侧导航栏、顶部 `proto-bar`、屏下「状态说明卡」与「交互与文案规则 / 设计标注」两栏 | 原型自己的阅读脚手架 | 不存在于产品界面 |
| `:root` 里的 `--platform-accent` / `--platform-accent-soft` | 还原意图色 | 不在本期补（§8.1 注、§10-D） |
| 日志进度百分比数值、`14:0x:xx` 时间戳、耗时秒数 | 示意值 | 由运行时真实值决定，**不作为验收对象**（BR-10 的区间划分归技术设计） |

---

## 9. 无障碍与响应式

**无障碍**

1. **三重标识**：状态以「颜色 + 图标 + 文字」表达（ui-design-spec 2.2.3）。阶段带除色以外还带**文字**「部署扩展」，ERROR 行除红色外还有 `[ERROR]` 与 `CircleClose` 图标——阶段判读不依赖颜色辨别。
2. **对比度**（WCAG 相对亮度比，按 `variables.scss` 实际值逐格计算）。**新组件的真实底是 `surface-2` / `surface-3`，不是页面底 `surface-0`**——v0.1 只算了 `surface-0` 一栏，于是一句「本期所有文字均在 AA 以上」对新区块并不成立，本版补齐四栏：

   | Token | `surface-0` #0b1722 | `surface-1` #10202d | `surface-2` #142735 | `surface-3` #1a3040 |
   | --- | --- | --- | --- | --- |
   | `--platform-text-primary` | 15.47:1 | 14.17:1 | 13.09:1 | 11.65:1 |
   | `--platform-text-regular` | 11.30:1 | 10.35:1 | 9.56:1 | 8.51:1 |
   | `--platform-text-secondary` | 6.45:1 | 5.91:1 | **5.46:1** | 4.86:1 |
   | `--platform-text-muted` | 4.00:1 | 3.67:1 | **3.39:1 ✗** | 3.01:1 ✗ |
   | `--platform-cyan` | 7.75:1 | 7.10:1 | 6.56:1 | 5.83:1 |
   | `--platform-amber` | 10.12:1 | 9.26:1 | 8.56:1 | 7.62:1 |
   | `--platform-green` | 9.14:1 | 8.37:1 | 7.73:1 | 6.88:1 |
   | `--platform-red` | 5.81:1 | 5.32:1 | 4.91:1 | 4.37:1 ✗ |

   AA 正文门槛 4.5:1。**`muted` 在 `surface-1` 起即不达标**；`red` 在 `surface-3` 上不达标（本期红色只出现在 `surface-0` 的日志行与 `surface-2` 的行底叠加，`surface-3` 上不用红字）。

   **一条本期要遵守的约束**：目标版本区块、下拉选项、步骤预览、摘要行、阶段带等**承载信息**的文字一律使用 `secondary` 及以上；`muted` 只用于装饰性 mono kicker（`rail-caption`、`section-kicker` 一类），且其内容必须在别处以合格对比度重述一遍。
   **本条自查改掉的三处（v0.1 违例）**：步骤预览的序号 `.no`、执行位置与致命性 `.pos`、下拉选项副标 `.ep-option small`——v0.1 用的都是 `muted`（落在 `surface-2` 上 = 3.39:1），本版一律改 `secondary`（5.46:1），其中「失败即终止 / 失败可继续」按语义取 `amber`（8.56:1）。
   **两处现状登记、本期不修**：① `.logs-container .log-time` 用 `muted`（`DeployProgress.vue:711-714` 同值，落在 `surface-0` = 4.00:1）。本期扩展阶段会让时间戳成为更吃重的核对锚点（同一步的三行靠它归组），但改它属既有实现的范围外改动，只登记不顺手修（§10-E）。② 既有 `el-empty`（E3「暂无日志」、I 态「请选择游戏查看详情」）同样用 `muted` —— 本条约束只管**本期新增**的元素，既有元素要保持「与改造前逐项一致」（AC-15 / AC-24 ④），因此**不得**为凑对比度去改它们：改了反而通不过回归核对。
3. **动效**：步骤点的 `transition: all .3s` 与 loading 旋转属非信息性动效；信息变化同时以文字呈现，满足 `prefers-reduced-motion` 降级。
4. **键盘与语义**：`el-select` 沿用 Element Plus 原生键盘操作（↑↓ / Enter / Esc）；区块标题与控件建立 `aria-labelledby` 关联；日志流容器 `role="log"` + `aria-live="polite"`，**但不得逐行播报**（2s 轮询节奏下会造成刷屏）；阶段带与 ERROR 行为状态变化点，其文字本身即播报内容。
5. **不依赖记忆**：致命性、既存键来源、恢复路径都以文字写在界面内，不要求用户记住上文（§6.1 的用词理由）。

**响应式**

| 断点 | 行为 |
| --- | --- |
| ≥ 1440（1920 主档） | 现状布局：`232px` rail + 内容区；步骤 2 为 `10 / 14` 分栏 |
| ≤ 1366 | 内容区收窄，`el-select` 保持 300px；步骤 2 分栏不换行 |
| ≤ 900（`deploy.vue:2685`） | rail 收窄至 190px，`stage-header` / `step-content` 横向内边距降为 18px；步骤 2 分栏换为单列 |
| ≤ 680（`deploy.vue:2699`） | rail 移到顶部、`stage-index` 缩到 22px；控件宽度 100% |
| 日志对话框 | 固定 700px 宽，视口 < 740px 时按既有实现收缩；日志行 `word-break: break-word`（既有）保证长文案换行不溢出 |

---

## 10. 交回 @Architect / Leader 的开放项（不自行取舍）

**A. 需 Leader 裁定并回写 PRD —— 已裁定（2026-09-20，随 PRD v0.6 回写）**

- **S1 下摘要行显示什么**：FR-04 字面「未选择时显示『默认版本』」与 AC-24 ①④ 在 S1 下互斥。本文取「不显示」，**Leader 已采纳**：口径为 §6.1 步骤 5 行改「摘要须显示所选版本（目录可用时；无可用目录时不渲染该行）」、FR-04 加同款限定；不单独出 PRD 增量，与 OP-05 并成 v0.6 一次回写。设计侧无需改动（P2 与 W7 / W8b 已按该口径成立）。

**B. 界面侧前提，需 @Architect 在设计中一并定（3 项，Leader 已收下转派）**

1. **`level → 视觉映射` 现状为失效** —— **Leader 已接受为新增 §14.2 待改依赖**（与 RISK-02 / RISK-03 同级），阻塞 AC-08 / AC-10 / AC-16 / KPI-02 的界面侧判定；归一化方案（后端统一小写 / 前端兼容大写 / 复用 `stage` 约定，任选其一但须定稿）由技术设计给。`DeployProgress.vue:88-109` 的 `getLogClass` / `getLogIcon` 以**小写** `info/warning/error/success/debug` 为键（评审复核：前端键是 `warning` 而非后端的 `warn`，即大小写与拼写**双重**失配），而后端 `DeployService.LogEntry`（`DeployService.java:67`）输出**大写** `INFO / WARN / ERROR / SUCCESS`，全链路无归一化 ⇒ 非 INFO 行全部落 `log-info` 兜底、致命失败在日志里长得和普通行一样。本文 E4 屏即该映射**应当**成立时的核对帧。
2. **`imageTag` 落位失败时的界面后果**（§14.2 行 4 / RISK-12 / AC-14）：若某目录条目只声明 `imageTag`、无 `patches` / `scripts`，而 tag 又落不进 compose 生成，向导会让用户选一个「看上去可选、实际什么都不做」的版本。请判定该情形是否**不展示该条目**，或展示时必须标注不生效。本文的「步骤预览」在两栈皆空时会显示「该版本无部署扩展步骤」，但不足以挡住误选。
3. **阶段带与「扩展」步骤点的触发源**（§14.2 行 6 / BR-10）：本文 D-03 以「是否真的进入扩展阶段」触发（P3），不依赖百分比区间；请确认由 `status` / `statusText` / `stage` 哪个驱动。**本设计不要求分配百分比**，原型内数值已标「示意」。

**C-E. 登记类（不阻断）**

- **C.** `docs/design/ui-design-spec.md` §3.3 的「4 步向导」与代码 5 步不一致（`deploy.vue:28-34`；PRD §6.1 与代码一致）。Leader 判定：**本期不修、不改该规范文档**，是否另立 Issue 交人类 Owner。
- **D.** `--platform-accent` / `--platform-accent-soft` 未定义（§8.1 注，12 行使用）。Leader 判定：**本期不修**，仅登记。
- **E.** `.logs-container .log-time` 用 `--platform-text-muted`（4.00:1，`DeployProgress.vue:711-714` 同值）——既有实现的现状，本期不修（§9.2）。

---

## 11. 覆盖对照

| PRD 界面侧要求 | 本设计落点 |
| --- | --- |
| FR-01 / AC-01 展示版本选择项 | §3.1 步骤 2、D-01、状态 E/F/G、W3-W5 |
| **AC-01「选项与目录 `versionId` 一致」的机械核对口径** | §6.1「选项主文本 / 选项副标 / AC-01 的机械核对口径」三行：核对对象 = **每个选项内可见的 `versionId` 副标**，集合逐项相等即通过；`displayName` 生效与否都不影响该判定 |
| **PRD §8.1 `displayName`（文案归 @Designer）** | §6.1 选项主文本 = `displayName`（缺省回退 `versionId`）；默认条目**有意不用**（§6.1「为什么是有意为之」）；目录不可用时 displayName 无从解析 → 摘要与日志回到 `versionId`（§6.3） |
| FR-02 / AC-02 / AC-24 / BR-11 缺口期与现状一致 | §5-P1、§6.4、状态 A/B/C/K、W1 + LB |
| FR-03 / §8.4.2 三态与不变式 | §5 三值规则、§4.1 的 S1/S2/S3 对应 |
| FR-04 / AC-01 摘要显示所选版本 | D-02、§5 取值表、W7-W9（含交回项 A） |
| FR-10 / FR-11 / AC-04 阶段位置与停实例 | D-03、§6.2 停实例三行 |
| FR-17 / FR-18 / AC-08 / AC-10 失败语义可见 | §6.2 / §6.3、状态 N/O、LC-LD |
| FR-19 / BR-14 回滚与保留边界 | §6.3 回滚两行 + 保留边界行、LH |
| FR-20 / AC-03 / AC-16 / KPI-02 可观测 | D-03 三处、§6.2、§8.2 阶段带 |
| BR-12 / AC-20 两类可辨识原因 | §6.3 两分支 + 区分锚点、LE-LF |
| BR-16 / AC-27 既存键存续的界面侧可见性 | D-04（H 态摘要行）、W9；界面侧**不**提供任何解除入口（§7 与 §6.3 红线） |
| §12 全部 15 行 | 逐行映射到 §4 状态表 + §6.3 文案（含「回滚失败」「多步骤中途失败」「向导不展示但键已存在」三行） |
| OP-05 四项（落位 / 阶段名 / 失败提示 / 单值是否隐藏） | §2-①②③ + §2-① 末条，全部定稿 |

## 12. 修订记录

| 版本 | 日期 | 变更 | 作者 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-20 | 首版：基于 PRD v0.5 @ `270f9d0` 与 ADR-0029 @ `590af8d` 产出向导（步骤 2 / 步骤 5）与部署日志（扩展阶段）两屏 HTML 原型（22 个状态），定稿 OP-05 三项；登记 1 项需 Leader 裁定的 FR-04 冲突、3 项交 @Architect 的界面侧前提（含 `level → 视觉映射` 现状失效）、2 项不阻断登记。未改动 `prd.md` 与任何功能代码。 | Designer |
| **v0.2** | 2026-09-20 | 处置专业评审第 1 轮（`f6312ef`，FAIL：4 阻断 + 7 建议）：**MF-1** 按 Leader 裁定 1 定稿选项文本（`displayName` + 恒渲染的 `versionId` mono 副标）与 AC-01 核对口径，默认条目吞 `displayName` 明写为有意为之；**MF-2** `l3`(LD) 阶段带提到「进入部署扩展阶段」之前并补齐停实例两行，X-07 写成「顺序即核对对象、任何屏不得例外」；**MF-3** 三处 `muted` → `secondary`、致命性词面取 `amber`，§9.2 对比度表补 `surface-1/2/3`；**MF-4** 补屏三处：W8b（S2 摘要「默认版本」）、LJ（全部成功完成终态）、E3/E4 拆分（空日志 / level 着色），屏数 22 → **25**。建议项 S-1～S-7 全部并入（W5 展开态、W6 `ElMessage.info` 实画、`.new-flag`→`.review-flag` 并声明为评审标记、`--platform-amber` 表述、删 `.ep-tag--info`、LH/LI 补锚行、`label` 缺省回退、§4.1 I 不可达）。OP-05 三项主体结论**未改**；§10-A 与 §10-B1 更新为「已裁定 / 已登记」。**未改动 `prd.md` 与任何功能代码。** | Designer |

---

## 13. 评审条目 → 落点（第 1 轮 `f6312ef` 复审对照）

| 评审条目 | 处置 | 落点（本文 / 原型） |
| --- | --- | --- |
| **MF-1** `displayName` 选项文案未定稿 | **采纳（按裁定 1）** | §6.1 新增「选项主文本 / 选项副标 / AC-01 口径 / 默认项 / 为什么是有意为之」五行；§2-① 配套结论；§5 摘要取值表；§11 两行；原型 W3 / W4 / W5 / W8 / W8b 选项与摘要均为「主文本 + `versionId` 副标」两行式，W3 第 3 项即 `displayName` 缺省 → 主副同值的样例 |
| **MF-2** LD 阶段带顺序违例 + 省两行 | **采纳** | 原型 `l3`：`BAND()` 移至「进入部署扩展阶段」之前，补「正在停止实例…」「实例已停止」两行，并补齐与前序屏同构的 `开始部署 / [DEPLOY] 部署完成` 首两行；§7 X-07 补「带 → 进入行 的顺序即核对对象，任何屏不得例外」；§3.2-3、§6.2 第一条规则同步 |
| **MF-3** 三处 `muted` 低于 AA | **采纳** | 原型 `.step-list .no` / `.step-list .pos` / `.ep-option small` → `secondary`；致命性词面拆出 `.pos .fm` 取 `amber`（「宿主机 / 容器」属执行位置不随之着色）；§9.2 换成 4 栏对比度表（含 `surface-2` 5.46:1 / 3.39:1 两格），并注明 `.log-time` 属现状不修 → 登记 §10-E |
| **MF-4 ①** 空日志 0 命中 + 「左/下」两面对照不成立 | **采纳（走 (a) 补屏支）** | 原型 E3 拆为 **E3 空日志**（`lines:[]`，真渲染 `el-empty「暂无日志」`）+ **E4 level 着色**；「左/下」措辞删除；§4.2 T → E3、D-07 → E3 / E4 |
| **MF-4 ②** 摘要「默认版本」分支不可达 | **采纳（走 (b) 补屏支）** | 原型新增 **W8b**（`catalog:'ready-default'` → `else val='默认版本'` 这一支第一次真被走到）；W8 屏名与 `state` 从「S2 / S3」收成 S3；§5 取值表加「原型屏」列 |
| **MF-4 ③** 「全部成功」无屏 + LA 名实不符 | **采纳（补屏 + 改名双管）** | 原型新增 **LJ**（`completed:true, success:true`，页脚「完成」）；LA 改名「扩展三步逐条成功（阶段执行中）」并去掉孤立的 AC-04；LD 改为完成终态帧，使「照常完成」有据可核；§4.2 M 行 → LJ、O 行标注完成终态 |
| **S-1** W5 未见单值展开 / W6 未见回落提示 | **采纳** | W5 加 `open:true`；W6 改为回落后的 `ready-default` + 实画 `.ep-message`（顶部居中 info toast），X-02 的「不得静默改选」从此有可见对象 |
| **S-2** `.new-flag` 未声明、会被照抄 | **采纳（改名 + 声明 + 列入不渲染清单）** | 原型 `.new-flag`「NEW」→ `.review-flag`「评审标记·非产品元素」（虚线描边、无实心底）；新增 **§8.4 原型脚手架清单（实现侧一律不输出）**；§6.4 末行加入「任何 NEW / 评审标记类徽标」 |
| **S-3** `--platform-amber` 写作「新增」 | **采纳** | 原型标注栏改为「**本期新增使用**（token 本身早已定义于 `variables.scss`）」；§8.1 标题与 amber 行同步，并按评审建议写死「原型自补 `--platform-accent` 仅为还原意图色，实现侧按 §14.2 结论处理」 |
| **S-4** `.ep-tag--info` 无定义 | **采纳（删）** | 原型 `shell()` 的页头主机标签改用基类 `.ep-tag`；全文件 `.ep-tag--info` 命中 0 |
| **S-5** LH / LI 缺「进入行」 | **采纳（不辩称截取，直接补锚行）** | 两屏补齐 `带 → 进入部署扩展阶段 → 停实例两行`；同时把「速查帧 ≠ 一次真实时序」写进该两屏 `diff` 与 §4.2 注，避免被读成顺序例外 |
| **S-6** `label` 缺省时的渲染回退 | **采纳** | §6.2 规则第 2 条：缺 `label` → 「补丁替换 〈序号〉」/「脚本执行 〈序号〉」，不让声明侧的 `PATCH` / `SCRIPT` 上界面；§6.4 禁用清单同步；W4 标注栏加「label 缺省回退」一条 |
| **S-7** §4.1 I 的「未选部署方式」支 | **采纳（按裁定 2：该支不可达，只登记）** | §4.1 I 触发条件收成「未选游戏」，表后加注（A–J 表格之后、§4.2 之前）：该支不可达，依据 `deploy.vue:65`（`ref("docker")`）与 `:273`（回落 `supportMethods[0]` 为空时取 `"docker"`）；不加任何文案或控件 |
| **不得动**：OP-05 三项主体结论 / `prd.md` / 编造口径 | **遵守** | 落位步骤 2、单值不隐藏控件、默认项不显示 tag、「部署扩展」命名、失败文案两分支锚点与恢复顺序 —— 全部原样；本次只动 `docs/ui/MERC-3/` 两文件，`prd.md` 与功能代码零改动；复扫：`http(s)://` 0、镜像 tag / 版号 0、真实路径 0，界面文字一律 `〈…〉` 槽位 |
