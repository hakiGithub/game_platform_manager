# 游戏服务器统一管理平台 — 项目进展报告

> 生成时间：2026-07-11
> 数据来源：代码目录结构、数据库建模文档、需求文档、issues.json、Git 提交记录

---

## 一、总体进度概览

| 维度 | 状态 |
|------|------|
| 立项阶段 | 阶段一「MVP 核心底座」进行中 |
| 后端框架 | ✅ Spring Boot 3 + MyBatis-Plus + SQLite + PF4J 已搭建 |
| 前端框架 | ✅ Vue 3 + Element Plus + Vite + Wujie 微前端已搭建 |
| 数据库建模 | ✅ 9 张核心表 SQL 全部设计完成 |
| 三大部署适配器 | ✅ LinuxGSM / Docker / Docker Compose 代码已存在 |
| 插件化体系 | ⚠️ PF4J + Wujie 已集成，但子应用加载存在回归 bug |
| 构建/启动 | ⚠️ 根目录 `mvn spring-boot:run` 失败（未完全修复） |
| 版本控制 | ⚠️ 大量代码未提交（Git 仅有 2 次提交，backend/frontend 均为 modified） |

**结论**：核心功能模块的代码骨架已基本落地，6 大模块均有对应后端 Controller / Service 和前端页面；但目前处于「能搭起来、尚未充分验证」的状态，存在 2 个 reopened 问题（其中 1 个为 Wujie 子应用加载回归），且绝大多数改动尚未提交到 Git。

---

## 二、后端模块完成情况

代码位置：`backend/core/src/main/java/com/gameplatform/`

| 模块 | 关键文件 | 状态 | 说明 |
|------|----------|------|------|
| 系统管理与鉴权 | `AuthController`、`UserService`、`SystemController`、`ConfigService` | ✅ 已搭建 | 登录、JWT、全局配置、密码 |
| 主机纳管 | `HostController`、`HostService`、`ssh/` | ✅ 已搭建 | SSH 连接、状态监控、端口检测 |
| 部署引擎 | `adapter/`、`DeployService`、`docker/`、游戏元数据 | ✅ 已搭建 | 3 种部署适配器、元数据扫描 |
| 游戏实例管理 | `InstanceController`、`InstanceService`、`FileService`、`LogService` | ✅ 已搭建 | 生命周期、文件、日志 |
| 插件化扩展 | `plugin/`（15 个文件）、`PluginController`、`PluginService` | ⚠️ 已集成 | PF4J 框架 + 后端扩展点，但清单/前端加载有问题 |
| 备份与运维 | `BackupController`、`BackupService` | ✅ 已搭建 | 文件/数据库备份还原 |
| WebSocket | `websocket/` | ✅ 已搭建 | `/ws/ssh`、`/ws/instance/console`、`/ws/instance/log` |

后端整体目录完整度很高，6 大模块的 Controller/Service/Mapper/Entity 层级齐全。

---

## 三、前端模块完成情况

代码位置：`frontend/src/`

| 模块 | 页面文件 | 状态 |
|------|----------|------|
| 仪表盘 | `views/dashboard/` | ✅ |
| 主机纳管 | `views/host/` | ✅ |
| 游戏/元数据 | `views/game/` | ✅ |
| 实例管理 | `views/instance/`（index/detail/deploy） | ✅ |
| Docker | `views/docker/`（index/container） | ✅ |
| 插件扩展 | `views/plugin/`（index） | ⚠️ 依赖 Wujie 子应用加载（当前 bug） |
| 系统/登录 | `views/system/`、`views/login/`、`views/error/` | ✅ |
| 微前端集成 | `src/plugins/`（Wujie） | ⚠️ 已接入，子应用未渲染 |

前端 9 个视图目录齐全，并包含 `api/`、`stores/`、`router/`、`layouts/`、`components/` 等标准分层，以及 27 个测试文件。

---

## 四、插件化体系（亮点 + 当前风险）

- **架构**：平台主应用（Vue 3）+ 插件子应用（独立构建）通过 **Wujie 微前端** 动态加载；后端基于 **PF4J** 实现插件加载/卸载/启停。
- **已落地**：`backend/plugin-l4d2/`（求生之路2 插件，自带前端子应用）、`backend/plugin-template/`（插件模板）、`backend/plugin/`（插件框架）。
- **当前阻塞**（见下方 Issue #1）：实例详情「插件扩展 Tab」中的 Wujie 子应用未渲染，回归测试未通过。

数据库侧 `sys_plugin` 表已设计完成（插件生命周期管理）。

---

## 五、数据库

`数据库建模.md` 已交付完整方案，覆盖概念→逻辑→物理三层，并给出可直接执行的 SQLite 初始化脚本：

- 9 张表：`sys_user`、`sys_config`、`sys_plugin`、`sys_operation_log`、`host_info`、`game_metadata`、`game_instance`、`game_instance_custom_command`、`game_backup_record`
- 索引规范、敏感字段加密（BCrypt / AES-256）、逻辑删除、MySQL 兼容预留均已写明
- 已写入默认管理员（admin/admin123）、系统内置配置初始化数据

---

## 六、当前待解决问题（issues.json）

| ID | 标题 | 状态 | 优先级 | 备注 |
|----|------|------|--------|------|
| 1 | Wujie 子应用在实例插件 Tab 中未加载 | 🔴 reopened | high | 后端清单 `frontend.menus` 仍为 null，前端映射层字段不匹配；2026-06-24 回归未通过 |
| 2 | 后端插件清单接口返回中文乱码 | 🟢 closed | medium | 已修复（显式 UTF-8 解码），回归通过 |
| 3 | Maven 在 backend 根目录 `spring-boot:run` 失败 | 🟠 reopened | low | 仅修复 `plugin-l4d2` 的 skip，未处理 parent pom 也被执行 run goal，修复不完整 |

**重点**：Issue #1 是高优先级阻塞项，直接影响插件化体系这一核心卖点的可用性；Issue #3 影响开发体验（需从正确目录/模块启动）。

---

## 七、与里程碑的差距（阶段二 / 预留项，尚未开始）

依据 `需求澄清.md`，以下内容属于阶段二或预留，当前未实现：
- 插件热更新、插件市场、插件开发脚手架
- 可视化表单配置全能力（多格式配置文件自动解析生成）
- 定时/增量/远程对象存储备份
- 监控告警、崩溃自愈、实例克隆迁移、配置版本回滚
- 更多游戏适配（阶段一目标 3 款：幻兽帕鲁 / 求生之路2 / Minecraft；目前仅确认 l4d2 插件代码落地）
- 操作日志页面（后端有表与能力，前端 `views/system/` 是否含日志页需确认）

---

## 八、建议的下一步动作

1. **修复 Issue #1（高优先级）**：确认 `buildDefaultMenus()` 是否真正写入 `frontend.menus`，以及前端 `mapBackendMenusToFrontend()` 字段映射是否与后端响应结构一致；必要时先用最小复现验证清单接口返回体。
2. **修复 Issue #3**：在 parent pom 的 `spring-boot-maven-plugin` 增加 `<skip>true</skip>`，避免从根目录 run 时 parent 模块报“找不到主类”。
3. **提交代码**：backend/frontend 有大量未提交改动，建议按模块梳理后分批 commit，避免工作丢失。
4. **补齐游戏适配**：确认 palworld / mc 元数据与插件是否需要补齐以满足阶段一「3 款游戏」目标。

---

*注：以上完成度基于代码目录结构与文档推断，未实际运行测试用例，不能等同于“全部测试通过”。*
