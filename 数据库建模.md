# 游戏服务器统一管理平台 数据建模方案
本方案严格对齐需求文档的设计规范与MVP要求，基于轻量优先、高扩展性、安全合规的核心原则，完成从**概念数据模型→逻辑数据模型→物理数据模型**的全流程设计，默认适配SQLite 3.x（MVP阶段），同时预留MySQL全兼容能力。

---

## 一、模型设计核心原则
严格遵循需求文档的数据库建模规范，补充落地级约束：
1.  **极简MVP原则**：仅保留核心业务表，非核心功能预留扩展字段，不提前建表过度设计
2.  **全表通用字段强制规范**：所有业务表必须包含`id`（主键）、`create_time`（创建时间）、`update_time`（更新时间）、`is_deleted`（逻辑删除标记）4个通用字段
3.  **安全合规原则**：敏感字段强制加密存储，禁止明文入库；所有用户输入相关字段做防注入处理
4.  **高扩展原则**：所有表预留`ext_info` JSON扩展字段，支持无表结构变更的功能扩展
5.  **性能优先原则**：查询高频字段建立合理索引，唯一字段建立唯一索引，禁止冗余索引
6.  **双兼容原则**：表结构同时兼容SQLite 3.x与MySQL 8.x，最小化后续迁移成本

---

## 二、概念数据模型（CDM）
### 核心业务实体
| 实体名称 | 核心定位 | 业务归属模块 |
|----------|----------|--------------|
| 用户 | 平台登录鉴权主体，MVP单用户，预留多用户扩展 | 系统管理与鉴权 |
| 全局配置 | 平台全局参数、SSH/Docker通用配置存储 | 系统管理与鉴权 |
| 插件 | 平台扩展插件的生命周期管理 | 系统管理与鉴权、插件化扩展 |
| 操作日志 | 全量用户操作行为审计记录 | 系统管理与鉴权 |
| 主机 | 纳管的Linux主机，游戏实例运行载体 | 主机纳管 |
| 游戏元数据 | 游戏适配的标准化配置，实现零代码游戏接入 | 部署引擎 |
| 游戏实例 | 平台管理的最小单元，单台主机上的单个游戏服务端进程 | 游戏实例生命周期管理 |
| 实例自定义命令 | 游戏实例绑定的专属Shell操作命令 | 游戏实例生命周期管理 |
| 备份记录 | 游戏实例的文件/数据库备份还原记录 | 备份与运维辅助 |

### 核心实体关系
1.  用户 1:N 操作日志（一个用户对应多条操作记录）
2.  主机 1:N 游戏实例（一台主机可部署多个游戏实例）
3.  游戏元数据 1:N 游戏实例（一款游戏对应多个运行实例）
4.  游戏实例 1:N 自定义命令（一个实例可绑定多条自定义命令）
5.  游戏实例 1:N 备份记录（一个实例对应多条备份记录）

---

## 三、逻辑数据模型（LDM）
| 实体名称 | 主键 | 核心属性 | 外键关联 | 非空约束 |
|----------|------|----------|----------|----------|
| 用户 | 用户ID | 用户名、加密密码、昵称、用户状态、最后登录时间、最后登录IP | - | 用户名、密码 |
| 全局配置 | 配置ID | 配置键、配置值、配置名称、配置描述、是否系统内置 | - | 配置键、配置值 |
| 插件 | 插件ID | 插件唯一标识、插件名称、版本、描述、作者、主类路径、插件文件路径、状态、安装时间 | - | 插件唯一标识、插件名称、版本、主类路径、插件文件路径 |
| 操作日志 | 日志ID | 操作人ID、操作人名称、操作模块、操作类型、操作内容、请求IP、请求方法、请求URI、请求参数、响应状态、响应信息、耗时、操作时间 | 操作人ID关联用户ID | 操作人ID、操作模块、操作类型、操作内容、请求方法、请求URI、响应状态 |
| 主机 | 主机ID | 主机名称、IP、SSH端口、SSH用户名、加密SSH私钥、操作系统发行版、包管理器、主机状态、CPU/内存/磁盘使用率、最后心跳时间、SSH超时时间、重试次数、备注 | - | 主机名称、IP、SSH端口、SSH用户名、SSH私钥 |
| 游戏元数据 | 元数据ID | 游戏唯一编码、游戏名称、图标、描述、支持的部署方式、默认端口、环境依赖、配置表单Schema、自定义操作、元数据文件路径、状态、版本 | - | 游戏唯一编码、游戏名称、支持的部署方式、元数据文件路径 |
| 游戏实例 | 实例ID | 实例名称、主机ID、游戏编码、部署方式、部署路径、运行状态、进程PID、端口映射、卷挂载、环境变量、健康状态、在线玩家数、最后健康检查时间、各部署方式专属配置、备注 | 主机ID关联主机ID、游戏编码关联游戏元数据编码 | 实例名称、主机ID、游戏编码、部署方式、部署路径 |
| 实例自定义命令 | 命令ID | 实例ID、命令名称、命令内容、超时时间、排序、状态、备注 | 实例ID关联游戏实例ID | 实例ID、命令名称、命令内容 |
| 备份记录 | 备份ID | 实例ID、备份类型、备份名称、备份路径、源路径、文件大小、备份状态、还原状态、备份完成时间、最后还原时间、备注 | 实例ID关联游戏实例ID | 实例ID、备份类型、备份名称、备份路径、源路径 |

---

## 四、物理数据模型（PDM）
### 通用字段说明（所有业务表强制包含）
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT | 主键唯一ID | BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY |
| create_time | TEXT | NOT NULL DEFAULT (datetime('now','localtime')) | 记录创建时间 | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP |
| update_time | TEXT | NOT NULL DEFAULT (datetime('now','localtime')) | 记录最后更新时间 | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP |
| is_deleted | INTEGER | NOT NULL DEFAULT 0 | 逻辑删除标记：0-未删除，1-已删除 | TINYINT NOT NULL DEFAULT 0 |
| ext_info | TEXT | NOT NULL DEFAULT '{}' | 扩展信息，JSON格式 | JSON NOT NULL DEFAULT '{}' |

---

### 1. 系统用户表 `sys_user`
**核心用途**：存储平台用户信息，MVP阶段单用户，预留多用户扩展
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| username | TEXT | NOT NULL UNIQUE | 登录用户名，唯一标识 | VARCHAR(64) NOT NULL UNIQUE |
| password | TEXT | NOT NULL | 登录密码，BCrypt不可逆加密存储，禁止明文 | VARCHAR(128) NOT NULL |
| nickname | TEXT | DEFAULT '' | 用户昵称 | VARCHAR(64) DEFAULT '' |
| status | INTEGER | NOT NULL DEFAULT 1 | 用户状态：1-启用，0-禁用 | TINYINT NOT NULL DEFAULT 1 |
| last_login_time | TEXT | DEFAULT NULL | 最后登录时间 | DATETIME DEFAULT NULL |
| last_login_ip | TEXT | DEFAULT '' | 最后登录IP地址 | VARCHAR(64) DEFAULT '' |

---

### 2. 全局配置表 `sys_config`
**核心用途**：存储平台全局参数，key-value结构，支持动态配置
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| config_key | TEXT | NOT NULL UNIQUE | 配置键，唯一标识 | VARCHAR(128) NOT NULL UNIQUE |
| config_value | TEXT | NOT NULL DEFAULT '' | 配置值 | TEXT NOT NULL DEFAULT '' |
| config_name | TEXT | NOT NULL DEFAULT '' | 配置名称 | VARCHAR(128) NOT NULL DEFAULT '' |
| config_desc | TEXT | DEFAULT '' | 配置描述/备注 | VARCHAR(256) DEFAULT '' |
| is_system | INTEGER | NOT NULL DEFAULT 0 | 是否系统内置配置：1-是，0-否，内置配置禁止删除 | TINYINT NOT NULL DEFAULT 0 |

---

### 3. 插件信息表 `sys_plugin`
**核心用途**：管理平台插件生命周期，对齐PF4J插件体系规范
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| plugin_id | TEXT | NOT NULL UNIQUE | 插件唯一标识（PF4J pluginId） | VARCHAR(128) NOT NULL UNIQUE |
| plugin_name | TEXT | NOT NULL | 插件名称 | VARCHAR(128) NOT NULL |
| plugin_version | TEXT | NOT NULL | 插件版本号 | VARCHAR(32) NOT NULL |
| plugin_desc | TEXT | DEFAULT '' | 插件功能描述 | VARCHAR(512) DEFAULT '' |
| author | TEXT | DEFAULT '' | 插件作者 | VARCHAR(64) DEFAULT '' |
| plugin_class | TEXT | NOT NULL | 插件主类全路径 | VARCHAR(256) NOT NULL |
| plugin_path | TEXT | NOT NULL | 插件文件存储路径 | VARCHAR(512) NOT NULL |
| status | INTEGER | NOT NULL DEFAULT 0 | 插件状态：1-启用，0-禁用 | TINYINT NOT NULL DEFAULT 0 |
| install_time | TEXT | DEFAULT NULL | 插件安装时间 | DATETIME DEFAULT NULL |

---

### 4. 操作日志表 `sys_operation_log`
**核心用途**：全量记录用户操作行为，满足审计需求，MVP必选
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| operator_id | INTEGER | NOT NULL | 操作人ID，关联sys_user.id | BIGINT NOT NULL |
| operator_name | TEXT | NOT NULL | 操作人用户名 | VARCHAR(64) NOT NULL |
| operation_module | TEXT | NOT NULL | 操作模块（如主机管理、实例部署） | VARCHAR(64) NOT NULL |
| operation_type | TEXT | NOT NULL | 操作类型（如新增、编辑、删除、执行） | VARCHAR(32) NOT NULL |
| operation_content | TEXT | NOT NULL | 操作内容详情 | TEXT NOT NULL |
| request_ip | TEXT | NOT NULL DEFAULT '' | 请求IP地址 | VARCHAR(64) NOT NULL DEFAULT '' |
| request_method | TEXT | NOT NULL | 请求方法（GET/POST/PUT/DELETE等） | VARCHAR(16) NOT NULL |
| request_uri | TEXT | NOT NULL | 请求URI | VARCHAR(256) NOT NULL |
| request_params | TEXT | DEFAULT '' | 请求参数，JSON格式 | TEXT DEFAULT '' |
| response_status | INTEGER | NOT NULL | 响应状态：1-成功，0-失败 | TINYINT NOT NULL |
| response_msg | TEXT | DEFAULT '' | 响应结果/异常信息 | TEXT DEFAULT '' |
| cost_time | INTEGER | NOT NULL DEFAULT 0 | 请求耗时，单位：毫秒 | INT NOT NULL DEFAULT 0 |
| operation_time | TEXT | NOT NULL DEFAULT (datetime('now','localtime')) | 操作执行时间 | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP |

---

### 5. 主机信息表 `host_info`
**核心用途**：存储纳管的Linux主机信息，平台底座核心表，MVP必选
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| host_name | TEXT | NOT NULL | 主机名称 | VARCHAR(128) NOT NULL |
| ip | TEXT | NOT NULL | 主机IP地址 | VARCHAR(64) NOT NULL |
| ssh_port | INTEGER | NOT NULL DEFAULT 22 | SSH服务端口 | INT NOT NULL DEFAULT 22 |
| ssh_username | TEXT | NOT NULL | SSH登录用户名 | VARCHAR(64) NOT NULL |
| ssh_private_key | TEXT | NOT NULL | SSH私钥，AES-256对称加密存储，禁止明文 | TEXT NOT NULL |
| os_distribution | TEXT | DEFAULT '' | 操作系统发行版（Ubuntu/Debian/CentOS等） | VARCHAR(64) DEFAULT '' |
| package_manager | TEXT | DEFAULT '' | 包管理器类型（apt/yum等） | VARCHAR(32) DEFAULT '' |
| status | INTEGER | NOT NULL DEFAULT 0 | 主机状态：1-在线，0-离线 | TINYINT NOT NULL DEFAULT 0 |
| cpu_usage | REAL | DEFAULT 0 | CPU使用率，单位：百分比 | DECIMAL(5,2) DEFAULT 0 |
| memory_usage | REAL | DEFAULT 0 | 内存使用率，单位：百分比 | DECIMAL(5,2) DEFAULT 0 |
| disk_usage | REAL | DEFAULT 0 | 磁盘使用率，单位：百分比 | DECIMAL(5,2) DEFAULT 0 |
| last_heartbeat_time | TEXT | DEFAULT NULL | 最后心跳检测时间 | DATETIME DEFAULT NULL |
| ssh_timeout | INTEGER | NOT NULL DEFAULT 30 | SSH连接超时时间，单位：秒 | INT NOT NULL DEFAULT 30 |
| ssh_retry_count | INTEGER | NOT NULL DEFAULT 3 | SSH连接重试次数 | INT NOT NULL DEFAULT 3 |
| remark | TEXT | DEFAULT '' | 主机备注 | VARCHAR(512) DEFAULT '' |
| **约束** | - | UNIQUE(ip, ssh_port) | 同一IP+SSH端口的主机不可重复纳管 | - |

---

### 6. 游戏元数据表 `game_metadata`
**核心用途**：存储游戏适配标准化配置，实现零代码新增游戏接入，MVP必选
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| game_code | TEXT | NOT NULL UNIQUE | 游戏唯一编码（如palworld、mc、l4d2），唯一标识 | VARCHAR(64) NOT NULL UNIQUE |
| game_name | TEXT | NOT NULL | 游戏全称 | VARCHAR(128) NOT NULL |
| game_icon | TEXT | DEFAULT '' | 游戏图标路径/URL | VARCHAR(512) DEFAULT '' |
| game_desc | TEXT | DEFAULT '' | 游戏描述 | VARCHAR(1024) DEFAULT '' |
| support_deploy_type | TEXT | NOT NULL | 支持的部署方式，JSON数组（1-LinuxGSM、2-Docker、3-Docker Compose） | JSON NOT NULL |
| default_port | INTEGER | DEFAULT NULL | 游戏默认服务端口 | INT DEFAULT NULL |
| default_dependences | TEXT | DEFAULT '[]' | 环境依赖列表，JSON数组 | JSON DEFAULT '[]' |
| config_schema | TEXT | DEFAULT '{}' | 配置表单Schema，JSON格式，用于前端自动渲染可视化表单 | JSON DEFAULT '{}' |
| custom_operations | TEXT | DEFAULT '[]' | 游戏自定义操作列表，JSON数组 | JSON DEFAULT '[]' |
| metadata_file_path | TEXT | NOT NULL | 元数据YAML文件路径 | VARCHAR(512) NOT NULL |
| status | INTEGER | NOT NULL DEFAULT 1 | 元数据状态：1-启用，0-禁用 | TINYINT NOT NULL DEFAULT 1 |
| metadata_version | TEXT | NOT NULL DEFAULT '1.0.0' | 元数据版本号 | VARCHAR(32) NOT NULL DEFAULT '1.0.0' |

---

### 7. 游戏实例表 `game_instance`
**核心用途**：平台管理的最小单元，存储游戏实例全量信息，MVP核心必选表
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| instance_name | TEXT | NOT NULL | 实例名称，同一主机下唯一 | VARCHAR(128) NOT NULL |
| host_id | INTEGER | NOT NULL | 所属主机ID，关联host_info.id | BIGINT NOT NULL |
| game_code | TEXT | NOT NULL | 关联游戏编码，关联game_metadata.game_code | VARCHAR(64) NOT NULL |
| deploy_type | INTEGER | NOT NULL | 部署方式：1-LinuxGSM、2-Docker单容器、3-Docker Compose | TINYINT NOT NULL |
| deploy_path | TEXT | NOT NULL | 实例部署根路径 | VARCHAR(512) NOT NULL |
| run_status | INTEGER | NOT NULL DEFAULT 0 | 运行状态：0-未部署、1-运行中、2-已停止、3-异常、4-部署中、5-卸载中 | TINYINT NOT NULL DEFAULT 0 |
| process_pid | INTEGER | DEFAULT NULL | 游戏服务进程PID | INT DEFAULT NULL |
| port_mapping | TEXT | DEFAULT '{}' | 端口映射，JSON格式 | JSON DEFAULT '{}' |
| volume_mount | TEXT | DEFAULT '[]' | 卷挂载列表，JSON数组（Docker/Compose专用） | JSON DEFAULT '[]' |
| env_vars | TEXT | DEFAULT '{}' | 环境变量，JSON格式 | JSON DEFAULT '{}' |
| health_status | INTEGER | DEFAULT 0 | 健康检查状态：1-健康、0-不健康 | TINYINT DEFAULT 0 |
| online_player_count | INTEGER | DEFAULT 0 | 当前在线玩家数 | INT DEFAULT 0 |
| last_health_check_time | TEXT | DEFAULT NULL | 最后健康检查时间 | DATETIME DEFAULT NULL |
| linuxgsm_script_name | TEXT | DEFAULT NULL | LinuxGSM脚本名称（LinuxGSM部署专用） | VARCHAR(128) DEFAULT NULL |
| docker_image | TEXT | DEFAULT NULL | Docker镜像地址（Docker部署专用） | VARCHAR(256) DEFAULT NULL |
| docker_container_name | TEXT | DEFAULT NULL | Docker容器名称（Docker/Compose部署专用） | VARCHAR(128) DEFAULT NULL |
| compose_file_path | TEXT | DEFAULT NULL | Compose文件路径（Docker Compose部署专用） | VARCHAR(512) DEFAULT NULL |
| remark | TEXT | DEFAULT '' | 实例备注 | VARCHAR(512) DEFAULT '' |
| **约束** | - | UNIQUE(host_id, instance_name) | 同一主机下实例名称不可重复 | - |

---

### 8. 实例自定义命令表 `game_instance_custom_command`
**核心用途**：存储游戏实例的自定义Shell命令，MVP必选功能
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| instance_id | INTEGER | NOT NULL | 所属实例ID，关联game_instance.id | BIGINT NOT NULL |
| command_name | TEXT | NOT NULL | 命令名称（前端按钮显示名称） | VARCHAR(128) NOT NULL |
| command_content | TEXT | NOT NULL | 命令执行内容，Shell脚本 | TEXT NOT NULL |
| timeout | INTEGER | NOT NULL DEFAULT 60 | 执行超时时间，单位：秒 | INT NOT NULL DEFAULT 60 |
| sort | INTEGER | NOT NULL DEFAULT 0 | 排序号，越小越靠前 | INT NOT NULL DEFAULT 0 |
| status | INTEGER | NOT NULL DEFAULT 1 | 命令状态：1-启用、0-禁用 | TINYINT NOT NULL DEFAULT 1 |
| remark | TEXT | DEFAULT '' | 命令备注 | VARCHAR(512) DEFAULT '' |

---

### 9. 备份记录表 `game_backup_record`
**核心用途**：存储游戏实例的备份还原记录，MVP必选基础能力
| 字段名 | SQLite数据类型 | 约束 | 字段注释 | MySQL兼容调整 |
|--------|----------------|------|----------|---------------|
| instance_id | INTEGER | NOT NULL | 所属实例ID，关联game_instance.id | BIGINT NOT NULL |
| backup_type | INTEGER | NOT NULL | 备份类型：1-文件备份、2-数据库备份 | TINYINT NOT NULL |
| backup_name | TEXT | NOT NULL | 备份名称 | VARCHAR(128) NOT NULL |
| backup_path | TEXT | NOT NULL | 备份文件存储路径 | VARCHAR(512) NOT NULL |
| source_path | TEXT | NOT NULL | 备份源路径/数据库名称 | VARCHAR(512) NOT NULL |
| file_size | INTEGER | NOT NULL DEFAULT 0 | 备份文件大小，单位：字节 | BIGINT NOT NULL DEFAULT 0 |
| backup_status | INTEGER | NOT NULL DEFAULT 2 | 备份状态：1-成功、0-失败、2-进行中 | TINYINT NOT NULL DEFAULT 2 |
| restore_status | INTEGER | NOT NULL DEFAULT 0 | 还原状态：0-未还原、1-还原中、2-还原成功、3-还原失败 | TINYINT NOT NULL DEFAULT 0 |
| backup_time | TEXT | DEFAULT NULL | 备份完成时间 | DATETIME DEFAULT NULL |
| restore_time | TEXT | DEFAULT NULL | 最后还原时间 | DATETIME DEFAULT NULL |
| remark | TEXT | DEFAULT '' | 备份备注 | VARCHAR(512) DEFAULT '' |

---

## 五、索引设计规范
严格遵循「高频查询必建索引、唯一字段必建唯一索引、禁止冗余索引」原则，MVP阶段索引设计如下：

| 表名 | 索引名称 | 索引字段 | 索引类型 | 核心用途 |
|------|----------|----------|----------|----------|
| sys_user | uk_username | username | 唯一索引 | 登录快速查询，保证用户名唯一 |
| sys_config | uk_config_key | config_key | 唯一索引 | 配置项快速获取，保证配置键唯一 |
| sys_plugin | uk_plugin_id | plugin_id | 唯一索引 | 插件快速查询，保证插件ID唯一 |
| sys_operation_log | idx_operator_time | operator_id, operation_time | 联合普通索引 | 按操作人+时间范围快速检索日志 |
| sys_operation_log | idx_module_type | operation_module, operation_type | 联合普通索引 | 按模块+操作类型快速筛选日志 |
| host_info | uk_ip_port | ip, ssh_port | 联合唯一索引 | 防止重复纳管主机，按IP+端口快速查询 |
| host_info | idx_host_status | status | 普通索引 | 快速筛选在线/离线主机 |
| game_metadata | uk_game_code | game_code | 唯一索引 | 游戏元数据快速查询，保证游戏编码唯一 |
| game_instance | uk_host_instance | host_id, instance_name | 联合唯一索引 | 保证同主机下实例名唯一，快速定位实例 |
| game_instance | idx_instance_host_id | host_id | 普通索引 | 快速查询某主机下的所有实例 |
| game_instance | idx_instance_game_code | game_code | 普通索引 | 快速查询某游戏的所有实例 |
| game_instance | idx_instance_run_status | run_status | 普通索引 | 按运行状态快速筛选实例 |
| game_instance_custom_command | idx_command_instance_id | instance_id | 普通索引 | 快速查询某实例的所有自定义命令 |
| game_backup_record | idx_backup_instance_id | instance_id | 普通索引 | 快速查询某实例的所有备份记录 |
| game_backup_record | idx_backup_create_time | create_time | 普通索引 | 按时间范围快速筛选备份记录 |

---

## 六、安全设计规范
1.  **敏感字段加密强制要求**
    - 用户密码：采用BCrypt不可逆加密算法存储，禁止明文、禁止可逆加密
    - SSH私钥：采用AES-256对称加密算法存储，加密密钥存放于平台环境变量/配置文件，禁止入库
    - 其他敏感信息（如数据库连接密码）：统一采用AES-256加密存储，禁止明文入库
2.  **逻辑删除机制**：所有业务查询默认过滤`is_deleted=1`的记录，仅数据清理场景可使用物理删除，避免数据误删无法恢复
3.  **防注入规范**：所有数据库操作通过MyBatis-Plus封装，禁止业务代码直接拼接SQL；所有用户输入参数必须经过校验与转义
4.  **审计不可篡改**：所有核心业务操作必须全量记录到操作日志表，禁止修改、删除历史日志记录

---

## 七、扩展预留设计
1.  **字段级扩展**：所有表预留`ext_info` JSON字段，支持非核心属性存储，无需修改表结构即可实现功能扩展
2.  **表级预留（二期迭代）**：MVP阶段不创建，避免过度设计，预留表包括：
    - 配置版本历史表`game_instance_config_history`（支持配置回滚）
    - 权限体系表`sys_role`/`sys_user_role`/`sys_permission`（多用户多角色扩展）
    - 定时备份任务表`backup_schedule`（定时备份能力扩展）
    - 主机分组表`host_group`（主机批量管理扩展）
3.  **兼容性预留**：所有表结构同时兼容SQLite与MySQL，字段命名、类型选择遵循通用数据库规范，避免数据库专属特性

---

## 八、MVP阶段初始化SQL脚本（SQLite 3.x）
```sql
-- 1. 系统用户表
CREATE TABLE IF NOT EXISTS sys_user (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT NOT NULL UNIQUE,
    password TEXT NOT NULL,
    nickname TEXT DEFAULT '',
    status INTEGER NOT NULL DEFAULT 1,
    last_login_time TEXT DEFAULT NULL,
    last_login_ip TEXT DEFAULT '',
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}'
);

-- 2. 全局配置表
CREATE TABLE IF NOT EXISTS sys_config (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    config_key TEXT NOT NULL UNIQUE,
    config_value TEXT NOT NULL DEFAULT '',
    config_name TEXT NOT NULL DEFAULT '',
    config_desc TEXT DEFAULT '',
    is_system INTEGER NOT NULL DEFAULT 0,
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}'
);

-- 3. 插件信息表
CREATE TABLE IF NOT EXISTS sys_plugin (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    plugin_id TEXT NOT NULL UNIQUE,
    plugin_name TEXT NOT NULL,
    plugin_version TEXT NOT NULL,
    plugin_desc TEXT DEFAULT '',
    author TEXT DEFAULT '',
    plugin_class TEXT NOT NULL,
    plugin_path TEXT NOT NULL,
    status INTEGER NOT NULL DEFAULT 0,
    install_time TEXT DEFAULT NULL,
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}'
);

-- 4. 操作日志表
CREATE TABLE IF NOT EXISTS sys_operation_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    operator_id INTEGER NOT NULL,
    operator_name TEXT NOT NULL,
    operation_module TEXT NOT NULL,
    operation_type TEXT NOT NULL,
    operation_content TEXT NOT NULL,
    request_ip TEXT NOT NULL DEFAULT '',
    request_method TEXT NOT NULL,
    request_uri TEXT NOT NULL,
    request_params TEXT DEFAULT '',
    response_status INTEGER NOT NULL,
    response_msg TEXT DEFAULT '',
    cost_time INTEGER NOT NULL DEFAULT 0,
    operation_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}'
);

-- 5. 主机信息表
CREATE TABLE IF NOT EXISTS host_info (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    host_name TEXT NOT NULL,
    ip TEXT NOT NULL,
    ssh_port INTEGER NOT NULL DEFAULT 22,
    ssh_username TEXT NOT NULL,
    ssh_private_key TEXT NOT NULL,
    os_distribution TEXT DEFAULT '',
    package_manager TEXT DEFAULT '',
    status INTEGER NOT NULL DEFAULT 0,
    cpu_usage REAL DEFAULT 0,
    memory_usage REAL DEFAULT 0,
    disk_usage REAL DEFAULT 0,
    last_heartbeat_time TEXT DEFAULT NULL,
    ssh_timeout INTEGER NOT NULL DEFAULT 30,
    ssh_retry_count INTEGER NOT NULL DEFAULT 3,
    remark TEXT DEFAULT '',
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}',
    UNIQUE(ip, ssh_port)
);

-- 6. 游戏元数据表
CREATE TABLE IF NOT EXISTS game_metadata (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    game_code TEXT NOT NULL UNIQUE,
    game_name TEXT NOT NULL,
    game_icon TEXT DEFAULT '',
    game_desc TEXT DEFAULT '',
    support_deploy_type TEXT NOT NULL,
    default_port INTEGER DEFAULT NULL,
    default_dependences TEXT DEFAULT '[]',
    config_schema TEXT DEFAULT '{}',
    custom_operations TEXT DEFAULT '[]',
    metadata_file_path TEXT NOT NULL,
    status INTEGER NOT NULL DEFAULT 1,
    metadata_version TEXT NOT NULL DEFAULT '1.0.0',
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}'
);

-- 7. 游戏实例表
CREATE TABLE IF NOT EXISTS game_instance (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    instance_name TEXT NOT NULL,
    host_id INTEGER NOT NULL,
    game_code TEXT NOT NULL,
    deploy_type INTEGER NOT NULL,
    deploy_path TEXT NOT NULL,
    run_status INTEGER NOT NULL DEFAULT 0,
    process_pid INTEGER DEFAULT NULL,
    port_mapping TEXT DEFAULT '{}',
    volume_mount TEXT DEFAULT '[]',
    env_vars TEXT DEFAULT '{}',
    health_status INTEGER DEFAULT 0,
    online_player_count INTEGER DEFAULT 0,
    last_health_check_time TEXT DEFAULT NULL,
    linuxgsm_script_name TEXT DEFAULT NULL,
    docker_image TEXT DEFAULT NULL,
    docker_container_name TEXT DEFAULT NULL,
    compose_file_path TEXT DEFAULT NULL,
    remark TEXT DEFAULT '',
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}',
    UNIQUE(host_id, instance_name)
);

-- 8. 实例自定义命令表
CREATE TABLE IF NOT EXISTS game_instance_custom_command (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    instance_id INTEGER NOT NULL,
    command_name TEXT NOT NULL,
    command_content TEXT NOT NULL,
    timeout INTEGER NOT NULL DEFAULT 60,
    sort INTEGER NOT NULL DEFAULT 0,
    status INTEGER NOT NULL DEFAULT 1,
    remark TEXT DEFAULT '',
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}'
);

-- 9. 备份记录表
CREATE TABLE IF NOT EXISTS game_backup_record (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    instance_id INTEGER NOT NULL,
    backup_type INTEGER NOT NULL,
    backup_name TEXT NOT NULL,
    backup_path TEXT NOT NULL,
    source_path TEXT NOT NULL,
    file_size INTEGER NOT NULL DEFAULT 0,
    backup_status INTEGER NOT NULL DEFAULT 2,
    restore_status INTEGER NOT NULL DEFAULT 0,
    backup_time TEXT DEFAULT NULL,
    restore_time TEXT DEFAULT NULL,
    remark TEXT DEFAULT '',
    create_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    update_time TEXT NOT NULL DEFAULT (datetime('now','localtime')),
    is_deleted INTEGER NOT NULL DEFAULT 0,
    ext_info TEXT NOT NULL DEFAULT '{}'
);

-- ------------------------------
-- 索引创建
-- ------------------------------
CREATE UNIQUE INDEX IF NOT EXISTS uk_username ON sys_user(username);
CREATE UNIQUE INDEX IF NOT EXISTS uk_config_key ON sys_config(config_key);
CREATE UNIQUE INDEX IF NOT EXISTS uk_plugin_id ON sys_plugin(plugin_id);
CREATE INDEX IF NOT EXISTS idx_operator_time ON sys_operation_log(operator_id, operation_time);
CREATE INDEX IF NOT EXISTS idx_module_type ON sys_operation_log(operation_module, operation_type);
CREATE UNIQUE INDEX IF NOT EXISTS uk_ip_port ON host_info(ip, ssh_port);
CREATE INDEX IF NOT EXISTS idx_host_status ON host_info(status);
CREATE UNIQUE INDEX IF NOT EXISTS uk_game_code ON game_metadata(game_code);
CREATE UNIQUE INDEX IF NOT EXISTS uk_host_instance ON game_instance(host_id, instance_name);
CREATE INDEX IF NOT EXISTS idx_instance_host_id ON game_instance(host_id);
CREATE INDEX IF NOT EXISTS idx_instance_game_code ON game_instance(game_code);
CREATE INDEX IF NOT EXISTS idx_instance_run_status ON game_instance(run_status);
CREATE INDEX IF NOT EXISTS idx_command_instance_id ON game_instance_custom_command(instance_id);
CREATE INDEX IF NOT EXISTS idx_backup_instance_id ON game_backup_record(instance_id);
CREATE INDEX IF NOT EXISTS idx_backup_create_time ON game_backup_record(create_time);

-- ------------------------------
-- 初始化默认数据
-- ------------------------------
-- 初始化默认管理员用户（用户名：admin，密码：admin123，首次登录强制修改）
INSERT INTO sys_user (username, password, nickname, status) 
VALUES ('admin', '$2a$10$7JB720yubVSZvUI0rEqK/.VqGOZTH.ulu33dHOiBE8ByOhJIrdAu', '系统管理员', 1)
ON CONFLICT(username) DO NOTHING;

-- 初始化系统内置配置
INSERT INTO sys_config (config_key, config_value, config_name, is_system) VALUES
('platform.name', '游戏服务器统一管理平台', '平台名称', 1),
('ssh.timeout', '30', 'SSH全局默认超时时间（秒）', 1),
('ssh.retry_count', '3', 'SSH全局默认重试次数', 1),
('docker.api_version', '1.43', 'Docker API默认版本', 1),
('backup.default_path', './data/backup', '默认备份存储路径', 1)
ON CONFLICT(config_key) DO NOTHING;
```