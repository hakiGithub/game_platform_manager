# 09 — 整轮编排 + 通过判定 + 报告完善

**What to build:** 收口成"一条命令回答过没过"：整轮 runner 按演练链顺序（l4d2 → dst → sdtd 腿 + 无主机用例组）编排执行；腿间与轮末强制实例清理；全局看门狗超时中止并产出部分报告；总报告输出总数/通过/失败/跳过（附原因）+ 失败截图 trace + **整轮通过判定（除显式 SKIP 外全部用例通过 = PASS）**。完善工程 README 四件事：怎么跑整轮/单组、凭据怎么配、怎么新增用例（页面 × 功能）、其他插件如何新增用例包。

**Blocked by:** 07, 08

**Status:** done

- [x] 一条命令跑完整轮并产出含通过判定的报告
- [x] 看门狗超时场景验证：中止并给出部分报告
- [x] 轮末牺牲主机与临时库零残留
- [x] README 覆盖：运行方式、凭据配置、新增用例指南、新插件用例包接入指南

> **最终整轮验证（PASS）**：`E2E_FAST=1 npm run e2e:round` 六组保序执行——冒烟与导航（9）→ 无主机页面组（10）→ 主机纳管（5）→ 实例生命周期 l4d2（6+2 缺陷跳过）→ 插件子应用（6+1 缺陷跳过）→ dst/sdtd 演练腿（4+2 环境跳过）。判定：除显式 SKIP 外全部用例通过 = PASS。分组结果经 runPlaywright 管道捕获并聚合；看门狗（E2E_ROUND_TIMEOUT，默认 150 分钟）超时中止剩余分组并输出部分报告。
>
> 实施事实：登录客户端加固（业务码严格校验 + 瞬时失败重试 2 次）——整轮演练曾因登录瞬时抖动导致演练组全链 401 空响应。
