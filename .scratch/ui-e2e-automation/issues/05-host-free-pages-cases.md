# 05 — 无主机页面组用例

**What to build:** 不依赖真实主机的页面全功能用例：游戏元数据（列表、编辑默认端口、YAML 导入从测试数据仓库取文件且导入前格式校验生效）、系统设置（加载、修改保存）、任务中心（列表/详情/筛选的页面结构与空态）、插件列表（显示 l4d2 已启动、点击"进入管理"弹实例选择框并断言跳转 URL 结构）。

**Blocked by:** 02

**Status:** done

- [x] 游戏元数据用例绿（列表/筛选/YAML 导入/删除清理）。偏差：页面**没有"编辑默认端口"功能**——元数据只能经 YAML 导入创建/更新，端口为只读展示，用例按实际功能组织（加载/筛选/导入/删除）
- [x] 系统设置改存生效 → **降级为"保存往返不报错"**。E2E 首轮发现产品缺陷：`GET /system/settings` 返回硬编码 VO、`PUT /system/settings` 未落库（SystemController 内 TODO 注释），设置保存是"假回环"；用例不断言持久化并在文件头记录缺陷，待产品修复后补断言
- [x] 任务中心结构用例绿（tab/筛选/空态与表格双兼容/详情容错态）
- [x] 插件列表用例绿（l4d2 卡片状态"已启动"、0 实例点进入工作区出未找到实例警告）
- [x] 整组不依赖真实主机，受管/附着模式均可跑

> 实施备注：游戏目录 139 个元数据分页展示，断言定位一律走关键词筛选；插件卡片状态显示后端 stateDesc 中文（"已启动"）；fixture YAML 必须满足 SnakeYAML 严格绑定（DockerConfig 字段为 image/tag，非 imageRepo/imageTag）。
