# 04 — 主机纳管用例

**What to build:** 主机模块全功能用例，打真实牺牲主机：新增主机走**连接测试门禁**（先测试连接成功才允许保存，填入环境契约里的主机信息）、主机列表渲染与操作、主机详情与资源监控数据渲染、Web 终端（连接成功 + 命令可输入；不对 xterm canvas 做输出文本断言）、编辑主机、删除主机、连接失败错误态（故意填错地址验证错误提示不崩溃）。整组受 SKIP 机制约束。

**Blocked by:** 02

**Status:** done

- [x] 新增主机用例真实通过连接测试门禁并落库，列表可见
- [x] 详情页资源监控有数据渲染
- [x] Web 终端可连上牺牲主机并接受输入
- [x] 错误凭据/地址时出现错误提示且页面不崩溃
- [x] 删除主机后列表更新
- [x] 无凭据环境整组 SKIP 并注明

> 实施备注：牺牲主机 = 本机 WSL（127.0.0.1:22 wslrelay 转发，haki/密码），凭据存 gitignored 的 e2e/.env.local（runner 经 support/env-file.js 装载，真实环境变量优先）。串行链 = 门禁真连保存 → 错误端口失败态 → 详情资源监控 → xterm 终端输入 → 输名确认删除，现场自清。直连后端的 API 路径必须带 /api 前缀（createApi 契约）。
