承接 MERC-8 的 tickets 拆解（stage 3 / 共 6 stage）。**由 Leader 在 stage 2 屏障关闭后 promotion。**

一句话：把「目标版本」做进部署向导——步骤 2 的控件、步骤 5 的摘要行、提交载荷那**唯一多出来的一个键**。控件读的是 MERC-18 已落地的 `deployVersions[]` 契约。

## 改哪里（`frontend/src/views/instance/deploy.vue`，design §7.4 F-04 / F-05，§3.3 组 H）

**1. F-04 步骤 2「目标版本」控件（ui-spec §2-① 落位、§4.1 十二条状态 A–J + F2 + G2、§5 谓词）**

- 落位 = **步骤 2「选择游戏」右侧详情面板，紧随「部署方式」之后**；沿用既有 `.detail-section` 区块形态与 `el-select` 的 `width: 300px` 惯例；**不新增卡片、不新增步骤、不改 `steps` 数组**。
- **P1 谓词只有一条**：控件渲染 ⇔ 目录可用（读取成功 **且** 条目数 ≥ 1，即 `deployVersions.length >= 1`）。**不得**把「载荷 `versionId` 是否在条目中」并进 P1（ui-spec §5 的 v0.5 MF-7 判定：那半只决定当前值怎么显示 = G2 态）。
- 切换部署方式须**重取目录**并回落（目录作用域 = `gameCode + deployType`，重取与回落须同屏可见）。
- 选项文本 = `displayName`（缺省回退 `versionId`）；**每条目录条目恒渲染 `versionId` mono 副标**；**「默认版本」恒为下拉第一项**（条目里存在 `default = true` 而不在首位时**渲染层上提**，不改声明、不做字典序重排）；无 `default = true` 条目时首位是**合成项**，**只显「默认版本」四字、不渲染副标**（**不得**为它编造副标）。默认项**有意吞掉 `displayName` 不用**。
- 步数徽标 / 选项行右侧步数按 §6.1「↳ 步数徽标」的三词面取，**`〈总数〉` 为 0 一律走「不执行」档**，禁止渲染「0 个步骤」。
- **G2 态**（目录可用但载荷 `versionId` 不在条目中）：按 ui-spec §6.1「↳ 控件当前值 G2」的**结果口径**实现——该 `versionId` 本体与「不在当前可选版本中」标识必须**同处一行 / 同段**可见，落点为控件值区或其紧邻一行；承载机制 (a)/(b) 二选一（**(a) 推荐**：值区只放组件回显值，warning 图标与那句短语一并落到其下紧邻的 `field-description` 行）；**两支都不许**：不给该值渲染 `versionId` 副标、不回落「默认版本」、不新增合成选项；该态下步数徽标与步骤预览**一律不渲染**。
- **两个稳定锚点必须落**（AC-01 核对程序的前提，§6.1 ③(b)）：**`data-ext-version-select`** 与 **`aria-label="目标版本"`**。选择器作用域**不得限定在向导容器内**（下拉可能传送到 `<body>`）。
- 单值目录**不隐藏控件**（§2-① 结论）。

**2. F-04 步骤 5 摘要行（ui-spec §5 取值表六行，逐行对应原型屏 W7/W8/W8b/W8c/W9/W9b）**

- **P2**：摘要行渲染 ⇔ P1 ∨ 提交载荷含 `deployVersion`；**S1（P1 假且无键）不渲染该行、也不显示「默认版本」**（Leader 已裁定，FR-04 加限定「（目录可用时）」）。
- **摘要行的有无与内容一律由提交载荷推导，不由目录状态推导**（§5 末段健壮性要求——否则「目录读失败但键既存」会被伪装成默认版本部署，那是 BR-12 要杜绝的结果在界面上的翻版）。
- 取值与下拉同源：同一主文本规则（`displayName ?? versionId`）+ 同一 `versionId` 副标；AC-01 的两句口径在步骤 2 与步骤 5 是**同一个机械核对对象**。

**3. F-05 提交载荷**

- 非默认选择 ⇒ 写 `configInfo.deployVersion`（值**精确等于**条目 `versionId`）；默认选择 ⇒ **省略该键**（删键由服务层 `applyVersionSelection` 承担，前端不做删除动作）。
- **载荷与现状的差异只多这一个键**；**不读不写不清理坏键 `gameVersion`**（D-N16 / F-11 / RISK-10）；不新增接口、不新增轮询通道。

## 本票要满足的 AC

| AC | 判据 | 出处 |
| --- | --- | --- |
| **AC-01** | 界面侧：控件出现、选项数 = 条目数、默认选中项 = `defaultEntry` 条目的 `versionId`、确认页摘要显示所选条目；**断言 locator 与 `versionId`/`displayName` 字段值，不匹配拼好的句子**。机械核对按 ui-spec §6.1「AC-01 两句口径 + 三条执行前提」：句一非默认选项副标**去重集合**相等（每元素恰 1 次，不是序列）、句二默认项单独判；**须先展开控件、展开后再取选项集合**，且**不得**把「收起 ⇒ 空集」写成断言 | V-21、§6.1 |
| **AC-02** | 停在默认版本 ⇒ 载荷**不存在** `deployVersion` 键，其余键与现状一致 | PRD AC-02、§8.4.2 S2 |
| **AC-24 ①②** | dnf-tw（目录 `EMPTY`）⇒ 向导**不出现**版本控件、载荷**不存在**该键；界面**无任何**示例版号 / URL / 路径 | PRD AC-24、ui-spec §6.4 |
| **AC-19 前端半** | 载荷键值精确等于所选 `versionId`（服务端判定归 MERC-18） | design §5.3 |
| **G2 / X-11 不回归** | 目录可用但值不在条目中：控件仍渲染、仍显该 `versionId` 与异常标识、提交仍携带该键，**不回落默认版本、不删键**——但「提交仍携带该键 ⇒ 被 BR-12 拦截」的端到端判定**归 stage 5 的 E2E 票（MERC-24）**，本票只判到载荷构造层 | ui-spec §5、§7 X-11 |

## 怎么验

```bash
cd frontend && npm run test:run     # 组件单测：P1/P2 谓词矩阵、选项排序（上提/合成项）、G2 两支承载、徽标 0 值档、载荷只多一键
cd frontend && npm run build
```

用 **MERC-14 的桩游戏外置元数据**（`plugin-stub` 目录 `AVAILABLE`）跑一次真实向导，逐项核对 AC-01 两句口径与摘要行六态；对照 ui-spec `@ 62b49a9` 的 W3 / W3b / W4 / W4b / W5 / W7 / W8 / W8b / W8c / W9 / W9b 屏。**端到端 E2E 用例不在本票**（stage 5 的「E2E 用例补齐」票负责），本票回传的是组件单测 + 一次手工核对记录。

## 依赖

**MERC-18**（`GET /api/games/{gameId}/deploy-config/{deployType}` 的 `deployVersions[]` / `versionCatalogState` 字段与 `applyVersionSelection` 的服务层三态）、**MERC-14**（`plugin-stub` + 桩游戏外置元数据 = 本票唯一能造出 `AVAILABLE` 目录的东西）。与 stage 3 的后端票（能力面）并行，文件面不相交（本票只动 `frontend/src/views/instance/deploy.vue`）。

## 权威文本与交付约定

- 权威文本：`ui-spec.md @ 62b49a9`（§2-① / §4.1 / §5 / §6.1 / §6.4 / §7 X-11 / §8.3）+ `design.md @ 790ccd6`（§7.4 F-04/F-05、§16.4）；PRD `@ ee86825` FR-01～FR-04、AC-01/AC-02/AC-24。
- 实现分支 `release/MERC-8-extension-steps-dnf-tw`；PR 目标分支同样是它；**PR 标题带 `MERC-8`**，正文**不得出现 `Closes` / `Fixes` / `Resolves` + Issue 编号**（G3 由人类收口）。**只快进、禁 force-push。**
- 回传三件：**变更文件清单** + **每条命令的完整输出** + **AC 对照表**，未达成项如实写。

## 两条「不编造」红线（违反即判本票不通过）

1. **OP-05 未关闭**（PRD §16.2）⇒ 本票是这条红线的**主战场**：控件标签只能叫「**目标版本**」（禁「游戏版本」「版本目录」）；默认项只能是「**默认版本**」四字（禁 `default`、禁镜像 tag、禁「（未选择）」）；选项主文本只能取 `displayName`/`versionId`，**目录里没有的词一个都不许造**（「最新版」「推荐版」「沿用模板版本」都是明禁项，见 §6.1 各行的「禁用替代词」列）。ui-spec §10-B 三项（B1/B2/B3）在 UI 侧仍标「**不并收、判门权在 Leader、未定稿前不得自行引入**」——遇到那三处**只登记缺口、不新造写法**。
2. **OP-02 资料缺口期**（BR-15）⇒ dnf-tw 只交付占位模板；**AC-05 / KPI-01 / KPI-03 记「不可测」**，不得用夹具、桩插件或示例版号 / 补丁 URL / 目标路径顶替。界面上出现的任何版本号只能是**桩游戏目录**的 `versionId`，AC-01 的通过**只计入框架类判定**，不得登记成 dnf-tw 的通过（AC-26 ①）；`plugin-dnf-tw` 未填充时向导必须**什么都没有**（AC-24 ①②⑥）。

## 分支纪律（运行时硬约束 · 2026-09-23 追加）

- 提交必须落在**你自己的任务分支**上（`multica repo checkout` 为你创建的那条，形如 `agent/<角色>/merc-NN`）；**不要在 worktree 里新建 `work/*` 分支并把 HEAD 留在那里**。
- 运行时只接受「HEAD 停在自己任务分支上」的交付：交付提交落在别的分支、任务分支停在基线时，本次 run 会被判失败、交付不被记账（MERC-18 的实际故障 —— 交付本身完好也一样不记账）。
- 需要中间分支时照常合并，但收尾前把自己的任务分支 fast-forward 到交付点，保持它与交付实物一致（MERC-19 的做法可参照）。
- 本票其余交付约定不变：合入 `release/MERC-8-extension-steps-dnf-tw`、只快进禁 force-push、PR 标题带 `MERC-8`、正文禁 `Closes`/`Fixes`/`Resolves`。
