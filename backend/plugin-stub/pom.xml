<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.gameplatform</groupId>
        <artifactId>game-platform-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <!--
      验收资产（design.md §3.3 组 K、PRD §5.1.1 验收资产行 / FR-24）：
      证明「除 dnf-tw 之外还有东西能声明扩展，且主应用无专用分支」（G-01 / AC-25）。

      刻意【不】挂在 backend/pom.xml 的默认 <modules> 上，只在 acceptance-assets profile 里出现
      ⇒ 日常的 mvn clean compile / package 与全量一键脚本都不会构建它，
      产品发布物因此不含桩插件（BR-15 ②、AC-26 ②）。构建方式见本模块 README.md。
    -->
    <artifactId>plugin-stub</artifactId>
    <packaging>jar</packaging>
    <name>Acceptance Asset - Stub Plugin</name>
    <description>部署扩展步骤框架的验收资产：最小桩插件，仅声明版本目录与混排步骤集，不含任何游戏语义</description>

    <properties>
        <plugin.id>plugin-stub</plugin.id>
        <plugin.class>com.gameplatform.plugin.stub.StubPlugin</plugin.class>
        <plugin.version>1.0.0</plugin.version>
        <plugin.provider>GamePlatform</plugin.provider>
        <plugin.description>部署扩展步骤框架验收桩插件（不随产品发布）</plugin.description>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.gameplatform</groupId>
            <artifactId>game-platform-plugin</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.gameplatform</groupId>
            <artifactId>game-platform-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.pf4j</groupId>
            <artifactId>pf4j</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <!-- PF4J 插件清单 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Plugin-Id>${plugin.id}</Plugin-Id>
                            <Plugin-Class>${plugin.class}</Plugin-Class>
                            <Plugin-Version>${plugin.version}</Plugin-Version>
                            <Plugin-Provider>${plugin.provider}</Plugin-Provider>
                            <Plugin-Description>${plugin.description}</Plugin-Description>
                        </manifestEntries>
                    </archive>
                </configuration>
                <executions>
                    <!-- 提前到 process-test-classes 再打一次 JAR 到 target/plugins/，
                         让 PF4J 真实加载用例在 test 阶段就能拿到插件 JAR。
                         带 classifier 且不 attach：否则与 package 阶段的 default-jar 撞同一坐标，
                         maven-jar-plugin 会拒绝「替换已发布的主构件」。 -->
                    <execution>
                        <id>stage-plugin-jar-for-load-test</id>
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/plugins</outputDirectory>
                            <classifier>staged</classifier>
                            <attach>false</attach>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
