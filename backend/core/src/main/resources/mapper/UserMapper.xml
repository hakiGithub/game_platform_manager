<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gameplatform.mapper.UserMapper">

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultType="com.gameplatform.entity.User">
        SELECT id, username, password_hash, jwt_secret, last_login_time, last_login_ip, 
               create_time, update_time, is_deleted, remark
        FROM sys_user
        WHERE username = #{username} AND is_deleted = 0
    </select>

</mapper>
