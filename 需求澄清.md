# 游戏服务器统一管理平台 项目需求与设计总览文档
**文档版本**：V1.0
**更新日期**：2026年03月21日
**适用对象**：UI设计师、前端开发、后端开发、数据库建模、测试工程师
**核心定位**：个人自用轻量型游戏服务器全生命周期管理平台，主打高扩展性、低维护成本、极简部署，支持多游戏适配与差异化管理。

---

## 一、项目核心概述
### 1.1 项目背景与目标
本项目为个人自用的游戏服务器统一管理平台，解决多游戏服、多主机、多部署方式分散管理的痛点，实现「一个平台纳管所有游戏服务器」，核心目标如下：
1.  统一纳管多台Linux主机，支持跨发行版兼容，采用SSH协议直连管理，预留后续扩展空间
2.  兼容多模式部署：支持LinuxGSM原生脚本、Docker单容器、Docker Compose编排三种部署方式，适配绝大多数游戏服务端的部署需求
3.  覆盖游戏服全生命周期管理：包括部署、启动、停止、重启、配置管理、文件管理、备份还原等基础运维能力
4.  支持差异化游戏管理：通过插件化体系，实现不同游戏的专属功能扩展（如求生之路2地图管理、网游GM工具、Mod管理等），支持无限新增游戏适配
5.  架构极简：采用单体前后端分离架构，拒绝微服务与复杂集群设计，优先保证开发迭代效率与长期可维护性

### 1.2 核心设计原则（全团队必须严格遵守）
1.  **轻量优先原则**：所有功能优先满足个人自用核心需求，拒绝过度设计、冗余功能，MVP阶段仅实现核心必选能力，非核心能力预留扩展接口即可
2.  **插件化扩展原则**：通用能力下沉到平台核心，差异化游戏能力全部通过插件实现，新增游戏适配/专属功能不得修改平台核心代码
3.  **标准化兼容原则**：所有部署方式、游戏适配、插件扩展必须遵循统一的标准化接口，屏蔽底层差异，保证平台核心逻辑稳定
4.  **体验统一原则**：无论是原生功能还是插件功能，UI交互、视觉样式、操作逻辑必须完全统一，用户无感知差异
5.  **安全兜底原则**：所有用户输入必须做安全校验，禁止明文存储敏感信息，杜绝命令注入、越权操作等安全漏洞

### 1.3 术语解释
| 术语 | 全称 | 解释 |
|------|------|------|
| LinuxGSM | Linux Game Server Managers | 开源的Linux环境下游戏服务端一键部署管理脚本集合，平台核心兼容的部署方式之一 |
| 游戏实例 | Game Instance | 单台主机上部署的单个游戏服务端进程，是平台管理的最小单元 |
| 适配器 | Adapter | 平台封装的标准化接口，用于屏蔽不同部署方式、不同Linux发行版的底层差异 |
| 插件 | Plugin | 独立于平台核心的扩展模块，用于实现特定游戏的专属管理功能，支持热加载、即插即用 |
| 元数据 | Metadata | 用于定义游戏基础信息、部署配置、表单规则的YAML配置文件，实现零代码新增游戏适配 |
| MVP | Minimum Viable Product | 最小可行产品，项目第一阶段必须交付的核心可用版本 |

---

## 二、整体架构设计
### 2.1 架构总览
采用**前后端分离的单体分层架构**，单节点部署即可运行，无额外强依赖中间件，分层职责清晰，各角色协作边界明确，整体架构自上而下分为5层：
```
【接入层】前端页面 + HTTP API网关 + 鉴权中间件
    ↓
【应用核心层】业务逻辑核心模块，无状态，标准化接口设计
    ↓
【统一适配层】标准化扩展接口，屏蔽底层差异，核心扩展入口
    ↓
【基础设施层】SSH通信、数据库、文件管理、日志、Docker SDK等基础能力
    ↓
【被控主机层】Linux目标主机、Docker环境、游戏服务端实例
```

### 2.2 各层职责与协作角色
| 架构分层 | 核心职责 | 主要负责角色 | 协作关联角色 |
|----------|----------|--------------|--------------|
| 接入层 | 前端页面渲染、用户交互、API请求封装、鉴权会话管理、路由控制 | 前端开发、UI设计师 | 后端开发、测试工程师 |
| 应用核心层 | 业务逻辑实现、流程编排、参数校验、权限控制、数据处理 | 后端开发 | 前端开发、数据库建模、测试工程师 |
| 统一适配层 | 部署适配器、游戏适配接口、插件扩展点、配置解析适配 | 后端开发、前端开发 | 测试工程师 |
| 基础设施层 | 底层能力封装、SSH连接管理、Docker操作、数据库操作、文件传输 | 后端开发 | 数据库建模、测试工程师 |
| 被控主机层 | 目标主机环境、游戏服务端运行环境、依赖组件 | 后端开发 | 测试工程师 |

---

## 三、核心功能模块需求说明
### 3.1 模块总览
项目分为6个核心功能模块，其中「系统管理与鉴权、主机纳管、部署引擎、游戏实例管理」为MVP必选模块，「插件化扩展体系、备份与运维辅助」为MVP阶段实现基础能力、二期完善的模块。

### 3.2 系统管理与鉴权模块
#### 核心定位
平台的基础入口，负责用户鉴权、全局配置、插件管理、操作日志记录，满足个人自用的极简权限需求。
#### MVP必选功能
1.  单用户登录鉴权：支持用户名密码登录、JWT会话管理、会话超时自动退出、密码修改
2.  全局配置管理：平台基础参数配置、SSH全局参数配置、Docker全局配置、游戏元数据管理
3.  插件基础管理：插件列表查看、插件启用/禁用、插件基础信息展示
4.  操作日志：全量用户操作记录（操作人、操作时间、操作内容、操作结果），支持查看与检索
#### 预留扩展功能
多用户/多角色权限体系、双因素认证、操作审计
#### 涉及角色
UI设计师、前端开发、后端开发、数据库建模、测试工程师

### 3.3 主机纳管模块
#### 核心定位
平台的底座核心，负责Linux主机的纳管、连接管理、状态监控、基础能力适配，是所有游戏实例运行的载体。
#### MVP必选功能
1.  主机纳管：支持SSH密钥认证方式纳管主机，支持主机信息配置（IP、SSH端口、用户名、密钥、备注）、连接测试、新增/编辑/删除
2.  发行版兼容：支持Ubuntu、Debian、CentOS三大主流发行版，自动适配apt/yum包管理器差异
3.  主机状态管理：实时同步主机在线状态、基础资源信息（CPU、内存、磁盘使用率）、SSH连接保活与重试
4.  端口管理：主机端口占用扫描、端口冲突检测，部署/修改游戏实例端口时自动校验并拦截冲突配置
5.  基础终端能力：集成Web SSH终端，支持前端直连主机执行命令
#### 预留扩展功能
Agent纳管方式、内网穿透、主机分组、批量操作
#### 涉及角色
UI设计师、前端开发、后端开发、数据库建模、测试工程师

### 3.4 部署引擎模块
#### 核心定位
平台的核心能力模块，通过标准化适配器接口，屏蔽不同部署方式的底层差异，实现游戏服务端的统一部署、卸载、生命周期控制。
#### MVP必选功能
1.  标准化部署适配器：定义统一的生命周期接口，所有部署方式必须实现「环境校验、预部署、部署、启动、停止、重启、健康检查、卸载」9个核心方法
2.  三大部署方式适配：
    - LinuxGSM适配器：完整封装LinuxGSM脚本全量命令，支持一键部署、启停、更新、基础运维
    - Docker适配器：封装Docker SDK，支持镜像拉取、容器创建、端口映射、卷挂载、环境变量配置、全生命周期管理
    - Docker Compose适配器：支持compose文件在线编辑/上传、多服务编排、全生命周期管理
3.  游戏元数据体系：通过YAML配置文件定义游戏的基础信息、支持的部署方式、默认端口、环境依赖、部署配置、自定义操作，平台启动时自动扫描加载，实现零代码新增游戏适配
4.  部署流程控制：部署前环境校验、端口冲突检测、依赖自动安装；部署中进度实时反馈；部署失败自动清理残留资源，给出明确错误提示
#### 预留扩展功能
存量实例纳管、Windows主机部署适配、K8s部署适配
#### 涉及角色
前端开发、后端开发、数据库建模、测试工程师

### 3.5 游戏实例生命周期管理模块
#### 核心定位
平台的核心业务模块，负责已部署游戏实例的全生命周期运维管理，覆盖日常操作的所有核心需求。
#### MVP必选功能
1.  基础运维操作：支持实例的启动、停止、重启、卸载、健康状态实时同步、在线玩家数/进程状态展示
2.  配置管理：
    - 优先可视化表单配置：基于游戏元数据的config_schema，自动生成前端可视化表单，支持配置项修改、保存、生效
    - 兜底通用方案：无表单配置的游戏，提供配置文件在线编辑、上传、下载功能，支持所有主流格式配置文件
3.  文件管理：基于SFTP实现实例对应主机目录的文件上传、下载、新建、删除、重命名，支持游戏存档、Mod、配置文件的快速管理
4.  自定义命令：支持给单个游戏实例绑定自定义Shell命令，前端生成操作按钮，点击即可执行并实时查看输出结果
#### 预留扩展功能
配置版本回滚、实例克隆/迁移、定时任务、崩溃自动拉起自愈
#### 涉及角色
UI设计师、前端开发、后端开发、数据库建模、测试工程师

### 3.6 插件化扩展体系模块
#### 核心定位
平台长期扩展性的核心，实现差异化游戏的专属功能扩展，保证核心代码稳定的同时，支持无限新增游戏适配与专属能力。
#### MVP必选基础能力
1.  插件框架集成：基于PF4J实现轻量插件体系，支持插件的加载、卸载、启用、禁用、生命周期管理，插件与平台核心完全解耦
2.  标准化扩展点定义：
    - 后端扩展点：自定义API接口、实例生命周期钩子、配置解析适配、专属业务逻辑实现
    - 前端扩展点：预定义4个核心插槽，插件仅可在指定插槽注册内容，保证体验统一
      1.  实例详情Tab插槽：插件可注册新的Tab页（如GM工具、地图管理、Mod管理），自动融入实例详情页
      2.  全局侧边栏菜单插槽：插件可注册全局菜单，自动融入平台侧边栏路由体系
      3.  实例列表操作插槽：插件可在实例列表操作栏注册自定义按钮
      4.  实例配置页区块插槽：插件可在配置页注册自定义表单区块
3.  分层扩展方案：
    - 零代码轻量扩展：通过YAML元数据配置，实现简单表单、列表、操作按钮的自动渲染，无需写前端代码
    - 中度扩展：自定义脚本绑定，实现中等复杂度的专属操作
    - 深度扩展：独立插件开发，支持自定义Vue组件、专属API、复杂交互功能
4.  插件页面无缝融合：插件前端代码与后端Jar包一体化打包，主应用动态加载插件组件，共享全局能力与UI规范，保证与原生页面体验完全一致
#### 二期完善功能
插件热更新、插件市场、插件开发脚手架、前端页面深度扩展
#### 涉及角色
UI设计师、前端开发、后端开发、测试工程师

### 3.7 备份与运维辅助模块
#### 核心定位
平台的辅助运维模块，保障游戏数据安全，降低运维成本。
#### MVP必选基础能力
1.  通用数据库备份还原：支持MySQL、PostgreSQL、SQLite等游戏常用数据库的手动全量备份、还原、备份文件管理
2.  游戏存档/配置文件手动备份：支持指定目录的手动备份、还原、备份文件下载
#### 二期完善功能
定时备份、增量备份、远程对象存储备份、备份过期自动清理
#### 涉及角色
前端开发、后端开发、测试工程师

---

## 四、技术栈规范（全团队必须严格执行）
### 4.1 后端技术栈
| 技术选型 | 版本要求 | 核心用途 | 强制规范 |
|----------|----------|----------|----------|
| JDK | 17+ LTS版本 | 开发运行环境 | 禁止使用JDK 8及以下版本 |
| 核心框架 | Spring Boot 3.x 稳定版 | 后端单体框架 | 禁止引入Spring Cloud等微服务相关组件 |
| ORM框架 | MyBatis-Plus 最新稳定版 | 数据库操作 | 单表CRUD必须使用内置方法，禁止冗余SQL |
| 数据库 | SQLite 3.x | 主数据库 | MVP阶段仅使用SQLite，预留MySQL兼容能力 |
| 插件框架 | PF4J 最新稳定版 | 插件化体系实现 | 所有扩展功能必须通过插件实现，禁止修改核心代码 |
| SSH客户端 | Apache MINA SSHD 最新稳定版 | 主机SSH连接与SFTP文件管理 | 禁止使用JSch等不再维护的老旧组件 |
| Docker SDK | docker-java 最新稳定版 | Docker/Docker Compose操作 | 统一封装工具类，禁止业务代码直接调用SDK |
| 配置解析 | Jackson、SnakeYAML、ini4j | 多格式配置文件解析 | 统一封装解析工具类，保证解析逻辑一致 |

### 4.2 前端技术栈
| 技术选型 | 版本要求 | 核心用途 | 强制规范 |
|----------|----------|----------|----------|
| 核心框架 | Vue 3.x 稳定版 | 前端核心框架 | 必须使用Composition API + setup语法糖，禁止使用Options API |
| 构建工具 | Vite 最新稳定版 | 项目构建 | 禁止使用Webpack等老旧构建工具 |
| UI组件库 | Element Plus 最新稳定版 | 全局UI组件 | 所有页面必须使用Element Plus组件，禁止私自引入其他UI库 |
| 路由 | Vue Router 4.x | 前端路由管理 | 必须与后端权限体系对齐，预留插件路由扩展能力 |
| HTTP客户端 | Axios 最新稳定版 | API请求 | 必须统一封装请求拦截、响应处理、错误提示、鉴权Token自动携带 |
| Web终端 | xterm.js 最新稳定版 | Web SSH终端实现 | 统一封装组件，禁止重复开发 |

### 4.3 设计规范
| 规范类别 | 强制要求 |
|----------|----------|
| 设计基础 | 必须以Element Plus官方设计规范为基础，所有组件、交互、样式必须与Element Plus保持一致，禁止私自修改组件基础样式 |
| 主题规范 | 必须使用CSS变量定义全局主题色、圆角、间距、字体等通用样式，禁止在页面中写死色值/尺寸，保证插件页面可复用全局样式 |
| 布局规范 | 采用「侧边栏+顶部导航+内容区」的经典管理平台布局，内容区统一使用卡片式设计，页面间距、内边距必须遵循全局规范，保证所有页面布局统一 |
| 交互规范 | 表单、表格、按钮、弹窗、通知等交互必须与Element Plus原生交互完全一致，操作按钮必须有明确的状态反馈，危险操作必须有二次确认 |
| 插件页面规范 | 插件页面必须完全复用主应用的全局样式、组件、交互规范，禁止出现与主应用不一致的设计风格，必须在预留插槽内渲染，不得破坏主应用布局 |
| 响应式规范 | 必须适配1920*1080、1366*768等主流桌面分辨率，保证在不同分辨率下布局正常、无样式错乱 |

### 4.4 数据库建模规范
1.  **极简设计原则**：MVP阶段仅设计核心表，禁止过度分表、冗余字段，满足单用户场景需求即可，预留扩展能力
2.  **通用字段规范**：所有业务表必须包含`id`（主键）、`create_time`（创建时间）、`update_time`（更新时间）、`is_deleted`（逻辑删除标记）4个通用字段
3.  **字段规范**：字段命名采用下划线命名法，必须添加字段注释，禁止使用关键字作为字段名；敏感字段（如SSH密钥）必须加密存储，禁止明文入库
4.  **索引规范**：所有查询频繁的字段必须添加普通索引，保证查询效率；禁止冗余索引
5.  **核心表清单**：用户表、主机信息表、游戏元数据表、游戏实例表、插件信息表、操作日志表，预留扩展表空间

### 4.5 测试规范
1.  **核心流程优先**：必须优先保障「主机纳管→游戏部署→实例启停→配置修改」全核心流程的可用性，覆盖所有成功/失败场景
2.  **兼容性测试**：必须覆盖Ubuntu、Debian、CentOS三大发行版，覆盖LinuxGSM、Docker、Docker Compose三种部署方式，覆盖主流游戏的适配测试
3.  **安全测试**：必须覆盖命令注入漏洞检测、敏感信息加密存储校验、越权操作检测、SSH连接安全校验
4.  **异常场景测试**：必须覆盖主机离线、网络中断、部署失败、启停异常、插件加载失败等异常场景，验证平台的兜底处理能力与错误提示友好性
5.  **体验测试**：必须验证所有页面的样式一致性、交互统一性、操作流畅性，插件页面与原生页面无割裂感

---

## 五、项目分阶段里程碑与交付标准
### 5.1 阶段一：MVP核心底座（周期1-2个月）
#### 核心目标
实现平台核心可用版本，跑通全核心流程，满足基础的游戏服管理需求
#### 交付物与完成标准
1.  完成平台基础框架搭建，前后端项目初始化，数据库核心表设计落地
2.  完成系统管理与鉴权模块全功能开发与测试
3.  完成主机纳管模块全功能开发与测试，支持三大发行版纳管、SSH连接、端口检测
4.  完成部署引擎模块核心开发，实现三大部署方式适配器、游戏元数据体系
5.  完成游戏实例管理模块全功能开发与测试，支持实例全生命周期管理、配置管理、文件管理
6.  完成插件体系基础框架搭建，实现标准化扩展点定义、基础插件管理能力
7.  完成至少3款热门游戏（幻兽帕鲁、求生之路2、Minecraft）的适配验证
8.  全核心流程测试通过，无阻断性bug，可正常部署使用

### 5.2 阶段二：核心体验完善（周期2-3个月）
#### 核心目标
完善插件化体系、差异化游戏管理能力、运维辅助能力，覆盖绝大多数游戏的管理需求
#### 交付物与完成标准
1.  完善插件化体系，实现零代码Schema页面渲染、动态Vue组件加载、插件热更新
2.  完成可视化表单配置全能力开发，支持多格式配置文件自动解析与生成
3.  完成备份与运维辅助模块全功能开发，支持定时备份、全量/增量备份
4.  完成至少10款主流游戏的适配，开发至少2款游戏的专属插件（如求生之路2地图管理插件、网游GM工具插件）
5.  完善异常处理、错误提示、操作反馈，优化全流程用户体验
6.  全功能测试通过，兼容性测试覆盖主流发行版与部署方式，无严重bug

### 5.3 阶段三：长期扩展迭代（按需开发）
#### 核心目标
补齐高级能力，持续扩展游戏适配，优化长期维护性
#### 核心迭代方向
1.  新增游戏适配与插件更新，完善热门游戏的专属管理能力
2.  补齐配置版本回滚、实例克隆/迁移、定时任务、崩溃自愈等高级运维能力
3.  实现Mod/插件/地图的创意工坊一键拉取、版本管理、冲突检测
4.  完善监控告警能力，支持主机/实例状态监控、异常告警通知
5.  预留扩展能力落地，如存量实例纳管、Agent纳管方式、Windows主机支持

---

## 六、协作边界与红线规范
### 6.1 各角色核心职责
| 角色 | 核心职责 | 交付物 |
|------|----------|--------|
| UI设计师 | 页面原型设计、视觉设计、交互规范制定、插件页面设计规范落地、高保真设计稿输出 | 设计稿、设计规范文档、切图、交互说明 |
| 前端开发 | 前端项目搭建、组件封装、页面开发、API对接、插件前端扩展体系实现、兼容性适配、bug修复 | 前端项目代码、部署包、前端开发文档 |
| 后端开发 | 后端项目搭建、接口设计与开发、业务逻辑实现、底层能力封装、插件后端体系实现、数据库交互开发、bug修复 | 后端项目代码、可执行Jar包、API文档、后端开发文档 |
| 数据库建模 | 数据库表结构设计、字段规范制定、索引优化、SQL审核、数据模型迭代 | 数据库设计文档、表结构SQL文件、数据字典 |
| 测试工程师 | 测试用例设计、功能测试、兼容性测试、安全测试、异常场景测试、bug闭环验证、测试报告输出 | 测试用例、测试报告、bug清单、回归测试记录 |

### 6.2 项目红线规范（全团队必须严格遵守，禁止突破）
1.  禁止引入任何微服务相关组件，禁止过度设计，所有功能必须遵循轻量优先、MVP原则
2.  禁止修改平台核心代码实现差异化游戏功能，所有扩展必须通过插件/元数据配置实现
3.  禁止明文存储SSH密钥、用户密码等敏感信息，所有用户输入必须做安全校验与转义，杜绝命令注入漏洞
4.  禁止私自修改UI设计规范、组件基础样式，所有页面/插件页面必须遵循统一的设计规范，保证体验一致
5.  禁止私自修改技术栈版本、引入未在规范内的第三方依赖，新增依赖必须经过团队评审
6.  禁止将二期/预留功能提前到MVP阶段开发，必须严格按照里程碑节奏迭代，优先保障核心流程可用