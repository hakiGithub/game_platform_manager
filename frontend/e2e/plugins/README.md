# plugins/ — 插件用例包命名空间

每个插件的 E2E 用例一个子目录（如 `l4d2/`），内含该插件子应用页面的全部用例与专属 fixture。
约定（第一个样板见 l4d2 用例包，票 07 落地）：

- 包内自带前置 fixture 说明与清理钩子，跑完不留实例/数据残留
- 效果断言走平台 REST API（`support/api.js`），不直连 SSH
- 包外框架代码保持零改动 —— 新增插件用例包 = 新增一个子目录
