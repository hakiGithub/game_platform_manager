=== 01a0cf4a-240f-74bf-8250-95749f5104cb agent [部署扩展步骤预览]
复现。 3. **三处解释口径**（可能有别的读法，供 G2 评审裁定）：    - G2 说明句用「…而它不在**当前可选版本中**」（对齐 §6.4 / X-11 与本票正文引号里的标识短语）；§6.1 的「全文」此处写「当前**目录的**可选版本中」，并删掉句尾内部文档引用「（§6.3 分支 ②）」。    - 值区用 `el-select` 的 `#label` 插槽实现 §8.2 的两段式；§8.3(a) 的「不加插槽」按「不把异常标识塞进值区」执行（异常标识确实只在紧邻 `field-description`）。若判为必须零插槽，代价是收起态值区无法显示版号副标（与 §8.2 冲突）。    - §4.1 D（加载中）保留占位渲染（`P1 ∨ 读取中`）：与「P1 只有一条」字面有张力，但 W2 明确要求占位渲染，P1 是稳态判据。    - 未在 §6.1 登记的界面文字：「部署扩展步骤预览」「收起 / 展开」（X-03/X-04 只要求折叠行为）。    - 0 值非默认条目被选中时，摘要徽标显示「不执行部署扩展步骤」（依 §6.1「0 值一律走后一档」；该行「摘要徽标」只登记了「将执行」形）。 4. **一处顺带修正**：变量元信息与版本目录原先各发一次同一个 GET（进步骤 2 = 2 次请求），合并为一次读取；副作用是「选定游戏」也重取变量，顺带修掉「换游戏但部署方式未变 ⇒ 沿用上一个游戏变量值」的既有隐患。 5. **未做**：`versionStepKindText` 与 `DeployProgress.vue`（MERC-19）的 `STEP_KIND_LABELS` 是同一张 2 项映射；受「文件面只动 `deploy.vue`」约束未抽公共模块，登记为后续收敛项。E2E 用例归 stage 5 / MERC-24。未改 `prd.md`、未新增接口、未动后端。 6. **验收资产已按 AC-26 ② 回收**：外置 `games/stub.yml` 与 `plugins/plugin-stub-*.jar` 已删除（回收前后核对输出见 `00-evid

=== 01a0cf4a-240f-74bf-8250-95749f5104cb agent [摘要徽标]
*当前可选版本中**」（对齐 §6.4 / X-11 与本票正文引号里的标识短语）；§6.1 的「全文」此处写「当前**目录的**可选版本中」，并删掉句尾内部文档引用「（§6.3 分支 ②）」。    - 值区用 `el-select` 的 `#label` 插槽实现 §8.2 的两段式；§8.3(a) 的「不加插槽」按「不把异常标识塞进值区」执行（异常标识确实只在紧邻 `field-description`）。若判为必须零插槽，代价是收起态值区无法显示版号副标（与 §8.2 冲突）。    - §4.1 D（加载中）保留占位渲染（`P1 ∨ 读取中`）：与「P1 只有一条」字面有张力，但 W2 明确要求占位渲染，P1 是稳态判据。    - 未在 §6.1 登记的界面文字：「部署扩展步骤预览」「收起 / 展开」（X-03/X-04 只要求折叠行为）。    - 0 值非默认条目被选中时，摘要徽标显示「不执行部署扩展步骤」（依 §6.1「0 值一律走后一档」；该行「摘要徽标」只登记了「将执行」形）。 4. **一处顺带修正**：变量元信息与版本目录原先各发一次同一个 GET（进步骤 2 = 2 次请求），合并为一次读取；副作用是「选定游戏」也重取变量，顺带修掉「换游戏但部署方式未变 ⇒ 沿用上一个游戏变量值」的既有隐患。 5. **未做**：`versionStepKindText` 与 `DeployProgress.vue`（MERC-19）的 `STEP_KIND_LABELS` 是同一张 2 项映射；受「文件面只动 `deploy.vue`」约束未抽公共模块，登记为后续收敛项。E2E 用例归 stage 5 / MERC-24。未改 `prd.md`、未新增接口、未动后端。 6. **验收资产已按 AC-26 ② 回收**：外置 `games/stub.yml` 与 `plugins/plugin-stub-*.jar` 已删除（回收前后核对输出见 `00-evidence.md`）；本次启动的后端/前端只按 PID 停止（与 `multica daemon statu

=== 01a0d42a-20b3-7d3d-997e-abec1b520769 agent [登记.*缺口]
t 弹层里的控件（本仓库 el-select 一族）都照抄这四步，不必重推。  **B. 契约层 + 界面层双证据同源留档**  同一次会话里 curl 真后端读到 `versionCatalogState=AVAILABLE / count=3`，Playwright 读界面（附件 `00-evidence.md` §2）。结论可互证，且把 dnf_tw（`ABSENT`, entries=0）与桩游戏（`AVAILABLE`, 3 条）放进同一份日志对照。  可复用：后端字段决定的谓词（P1 这种），验收必须同时留「字段实测」+「渲染实测」两行；只留界面等于没有证据链。  **C. 差异化解读「登记而不自裁」**  三处可能有另一种读法的落点写进「已知限制」并注明「供 G2 评审裁定」，界面上不引入任何自造写法（对齐红线 1：目录里没有的词一个都不许造）。可复用：权威文本自相矛盾时，只登记缺口 + 保持最小写法，不就地发明。  **D. 验收资产回收 + 停服纪律**  附件 `00-evidence.md` §4：回收前后各核一遍（`grep exit=1 → 非 0 即通过`）；停服按 PID（java 9684 / node vite 16136），并与 `multica daemon status --output json` 比出 daemon pid=7052 未被波及。可复用：起过本地服务的票，收尾必留「回收到什么都没剩」的前后对照。  **E. 真环境插桩脚本一次性、跑完即删**，只留日志与截图进附件。  ## 2. 做错了什么、返工在哪（证据）  **返工 1（自检发现）：下拉两行式选项行不撑开。** `a32ee3f` 首版把 `displayName` 主文本与 `versionId` 副标压进同一行高。修正 `a387fc0` `fix(frontend): 自检修正——下拉选项行高随两行内容撑开（ui-spec §8.2）`。  **返工 2（自检发现）：G2 值区不是 mono。** 首版只有 `field-description` 里的 `co

