docs(design): MERC-3 技术设计 v0.3.11 — 四处同类相邻滞后续批（§1 + §15.1 的 timeoutMs 读法 / §7.2 B-13 + §8.4 的命令形状）

按 Leader 重派评论 01a0d45d 第 1 条授权，把 v0.3.10 登记为「只报不改」的四处同类相邻滞后按同一
纪律落文；判定对象、编号、核对物零改动，不新增任何事实（两族事实都已在 §15.2 v0.3.10 钉过）。

- 第一族（§15.2 那处 timeoutMs 旧读法在本文的另两处落点）：§1 引导句 :49、§15.1 首行 :932。
  executeCommand 的 timeoutMs 只流向 SshUtil:356 的 getOrCreateSession（→ :135 签名 → :152 的
  connect，即建连与认证），命令执行走 SshUtil:368 的 executeRemoteCommand、不带超时实参
  ⇒ 两参重载缺省的 30_000L（FileAccessService:264）是「建一条通道」的预算、不是「一条命令最多跑
  多久」的护栏。§15.1 那行的取值 30 s 与出处 FileAccessService.java:264 逐字未动，改的是行标签与
  「这枚量级锚点可被当作什么用」。
- 第二族（命令形状写作裸 bash <file> 的另两处落点）：§7.2 B-13 步骤列 :245、§8.4「脚本注入」行 :318
  补齐为 `timeout <秒> bash <file>`，与 §15.2（v0.3.10）、已交付实现（ExtensionScriptRunner:96，
  类注释 :34 自述「而不是裸 bash <file>」）及仓内同形先例（DockerComposeAdapter:257+:260、
  LinuxGsmDockerAdapter:221+:223，两处现场注释逐字写着「timeoutMs 仅作用于建连、命令执行无超时」）
  对齐。B-13 的核对物列（「命令文本里不出现脚本正文」「未校验通过不在宿主机留文件」）逐字未动。
- 判定正文被改写的既有行恰这 4 行、零纯插入行（§1–§16 行数 1061 → 1061）；元数据三格 + §0 性质注 +
  §17 自查块 + §18 行皆为记账，全文 1344 → 1366 行。
- 登记同族表述三处仍「只报不改」（超本批授权面，交 Leader 判）：§15.2「缺省值」行把 30 s 括注为
  「宿主机通道默认」、§15.3 首行与 §12 RISK-D06 行的「FileAccessService 的超时语义是『不再等待』」——
  两处「不保证远端进程已终止」的结论仍成立（远端 timeout 发 SIGTERM，脚本可自行 trap、子进程可存活），
  但归因方式与本批口径不再一致，改它们要动 §15.3 与 RISK-D06 的登记语义。
- 机器核对：verify-v0311.py 48 PASS / 0 FAIL；反向对照 --ref f3f3703 = 29 PASS / 19 FAIL（exit 1，
  「恰四行改写」「四处旧串归零」「正文新串精确计数」「正文内不再有裸命令形状」「无第 11 处改动」
  「原段内单段替换等式」六组在旧 ref 上全部不成立）；carrier-v0311.py 22 条被引用的代码事实逐条对
  main / release / 交付头核，全 PASS。表数 74→75、编号对称差为空（179/179）、列数不一致 0、
  缺分隔行 0、URL 0、x.y.z 集合不变、新增版本号字面量只含 v0.3.11。
- 顺带核出一条引注口径（本批未引入、登记待判）：本文代码行号一律钉在 main 上，release 头并入
  MERC-20 后 DockerComposeAdapter 的 compose-exec 那行由 :728 下移到 :781（同一语句、行号漂移）。
- 单独 commit、只触 docs/design/MERC-3/design.md、续在 f3f3703 之上、快进、禁 force-push。
